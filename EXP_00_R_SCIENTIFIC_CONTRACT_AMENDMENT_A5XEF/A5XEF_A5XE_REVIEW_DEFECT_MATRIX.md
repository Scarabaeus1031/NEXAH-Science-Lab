# A5XEF — A5XE Independent-Review Defect Matrix

**Creation-order record:** this matrix is the first A5XEF artifact. No A5XEF schema, machine contract, fixture, derivation, validator, test, report, or seal existed when it was created.

**Stop-condition result:** the accepted V1→A5 authority uniquely specifies every repair below. No new hypothesis, threshold, population, statistic, P1–P5 meaning, N1–N5 definition, classifier branch, or interpretation is required. All findings are Class-B encoding/derivation/integrity defects.

| ID | Authoritative source rule | A5XE failure | Exact Class-B repair | Raw artifacts | Consuming derivations | Validation invariant | Adversarial counterexample | Fail-closed behavior |
|---|---|---|---|---|---|---|---|---|
| F40 | V1 primary supported-triple analysis; A1 dominance population; A4/A5 support and validity gates | Primary/null models select all rows by split and ignore joint-support membership. | Derive named row sets from representation support inputs; make every model/statistic consume an explicit population ID/hash; keep the nonzero population validity-only. | rows, representation support inputs, population ledger | observed, nulls, bootstrap, attribution, N5-RUN, sensitivities | Every consumed row ID is exactly the derived population; unsupported favorable rows have zero influence outside allowed populations. | Keep 93.33% valid joint coverage but assign extreme favorable outcomes to the excluded 6.67%. | `INVALID_EXPERIMENT` for population binding mismatch; otherwise derive from the correct subset. |
| F41 | A4 P4; A5 P4 attribution contract and prospective choice | Four score/margin controls are absent from fitted design; report-only diagnostics are name placeholders. | Store raw T/F best scores and margins; consume all four in both primary carrier models; reconstruct T-only, F-only and equal-score-fusion diagnostics with frozen populations and outputs. | model rows, representation scores, ranks, outcomes | primary carriers, diagnostic models, P4 completeness | Design-column identity and values match raw inputs; exact three diagnostics exist with derived metrics/provenance; diagnostics cannot rescue/veto P4. | Delete/swap/fabricate a control; replace a diagnostic with a favorable summary. | Missing/wrong controls or diagnostics → `INVALID_EXPERIMENT`, no classification. |
| F42 | A4/A5 exact 12-sensitivity registry, one-factor and completeness contract | A5XE accepts supplied coherence arrays and ignores path/value/config/output contradictions. | Bind each ID to exact ordinal/path/values; derive variant config by one-leaf patch; verify unchanged-leaf and full hashes; reconstruct variant rows/support/models/outputs from lower-level evidence. | primary config, variant configs, variant row bundles, support/provenance | 12 sensitivity derivations, P5 | Exact registry/set/order; one changed leaf; canonical hashes; exact population/seed/row mappings; all required finite outputs; only amplitude variants enter P5. | Wrong path/value/hash, copied result, impossible seeds, missing output, changed unrelated leaf. | Any incomplete/contradictory sensitivity → `INVALID_EXPERIMENT`. |
| F43 | Frozen standardized L2-logistic specification and deterministic ordering in V1/A4/A5 | Raw `model_spec` records 12 iterations while both implementations hard-code 100 and ignore the record. | One canonical typed model spec is passed into every fit; no operative fit constant is duplicated outside it; derived model provenance binds the consumed spec hash. | canonical model spec | all observed/null/bootstrap/attribution/sensitivity fits | Changing any model field changes consumed behavior or invalidates; declared and executed iteration counts cannot differ. | `iterations=12` with code behavior 100; change C/feature order without output change. | Mismatch/unsupported model spec → `INVALID_EXPERIMENT`; preauthorization conformance defect → `IMPLEMENTATION_FAILURE`. |
| F44 | A1–A5 prose/machine isomorphism and executable validity requirements | `validate_machine_object` ignores operative model, observed, validity, fixed-population and sensitivity-output fields. | Enumerate every operative field in a semantic coverage ledger; validate typed values and bind each to raw source, consumer and downstream result; mutation-test each field family. | machine contract, coverage ledger | validator and both derivations | No classification-relevant field is presence-only, unchecked text, ignored, or contradicted by code. | Mutate model C, coherence definition, validity gates, fixed populations or sensitivity outputs while bypassing file hash. | Semantic validator rejects before derivation/classification. |
| F45 | Complete accepted V1→A5 authority stack; A5X/A5XR/A5XE finite-root boundary | A5XE claims transitivity but omits A1. | Seal all operative A1–A5 artifacts plus A5X/A5XR/A5XE roots/reviews used by A5XEF; independently verify nested members and external expected digests. | authority ledger, upstream roots/members, A5XEF root | pre-derivation authority gate | Every listed byte/member/composite matches; no operative upstream dependency is absent; coordinated local rewrite cannot change external expectations. | Mutate operative A1–A5 or A5X/A5XR/A5XE dependency in a temporary copy; rewrite a copied local root. | `IMPLEMENTATION_FAILURE`; release and scientific classification prohibited. |
| F46 | Future V3 must instantiate accepted contract; A5XE review registered-readiness requirement | A5XE derivation requires `SYNTH_00..29` and `fixture_identity=SYNTHETIC_ONLY`. | Define a namespace-neutral schema with explicit experiment, seed, split, row, query, representation, support, action, outcome, model, null, bootstrap, N5, attribution, sensitivity and provenance identities; parameterize registries in a sealed manifest. | generic evidence manifest plus raw tables | both complete derivations | Identity types/foreign keys are generic; canonical SYNTH and an alternate non-SYNTH synthetic namespace derive identically after identity normalization; actual registered registry remains unread. | Replace synthetic prefix with `ALT_FIXTURE_*`; introduce malformed/foreign generic ID. | Valid alternate namespace accepted; malformed/foreign identity → `INVALID_EXPERIMENT`. |

## Repair boundary

A5XEF may encode and test these already accepted rules only. If implementation exposes an unresolvable scientific choice, construction stops and `SCIENTIFIC_CONTRACT_AMBIGUITY.md` is created instead of guessing.
