# A5XEF P4 Attribution and Diagnostics Contract

For each primary T/F carrier, the baseline design is exactly state signal, T best score, T top-two margin, F best score, F top-two margin, carrier-action magnitude and sign. The augmented design appends coherence. Controls are calculated from raw five-action score vectors and are consumed in every fit; a presence assertion has no authority.

Both derivations reconstruct per-seed direction and exact rational dominance from joint TEST rows and frozen primary predictions. `G<=0` or `2D3>G` is a valid negative P4 result; equality passes.

T-only, F-only and equal-score-fusion diagnostics are independently fitted from raw scores/outcomes. They are mandatory for validity/provenance and report-only for classification: their numeric values cannot rescue or veto P4.
