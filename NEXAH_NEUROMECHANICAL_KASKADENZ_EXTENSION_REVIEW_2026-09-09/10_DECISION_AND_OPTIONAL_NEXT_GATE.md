# Decision and Optional Next Gate

## Main decision

`DECISION_CLASS=C_NEW_BOUNDED_NEUROMECHANICAL_FORMALIZATION_JUSTIFIED`

Exactly one main class is selected.

- Not A: no integrated local neuromechanical capability or test exists.
- More than B: the missing layer requires a new bounded state/delay/energy/information specification, not just wiring existing executables.
- Not D: the candidate is reducible to falsifiable standard-control and conventional-mechanics contrasts; mnemonic imagery is separately excluded.
- Not E overall: primary/authoritative science and the predecessor are sufficiently bound. E remains correct for EXP20 only.

## Highest-effect candidate

The smallest useful integration is:

    HUMAN AUTHORITY (intention/target/STOP)
      + OLS OBSERVE/COMPARE/VALIDATE/RECORD
      + OBSERVER/ILAU frame-and-return record
      + STANDARD delay-aware feedforward/feedback/revision wrapper
      + HASH-LOCKED BODY-ARMS-CLUB mechanical plant and energy ledger

This leaves ORION core, TITAN authority, THE EYE, NEXAHEDRON and closed workstreams unchanged. THE EYE/NEXAHEDRON are optional later presentation surfaces, not required scientific mechanisms. Representation Ledger V3 is optional provenance because its own final review did not authorize a validator.

## Optional next gate — not activated

`NMK01_MINIMUM_CONTROL_RETURN_WRAPPER_GATE` could implement only file 07 in a new directory after explicit Owner authorization. Preconditions: frozen predecessor hashes; target/frames/contact/events; per-channel delay model; fixed revision rule; equal-work accounting including `P_K`; separate information ledger; preregistered Q1-Q3 endpoints/failure states; no Human calibration; no EXP20; no ORION-core or product change.

The gate must stop on source/frame/latency/work/energy ambiguity. A successful synthetic result would establish only behavior of that bounded model and, separately, audit reconstructability. It could not establish population biomechanics, coaching efficacy, neural implementation, cross-domain identity or new science.

    NEXT_GATE_STATUS = OPTIONAL_NOT_AUTHORIZED
    IMPLEMENTATION_AUTHORIZED = NO
    SAFE_TO_PAUSE = YES

