# EXP-00-R Authoritative Scientific Contract Amendment A3

## Authority and boundary

A3 prospectively repairs A2-R08–A2-R15. Authority order after independent acceptance is frozen V1, accepted A1, accepted A2 rules, then A3 corrections. A3 does not reopen the seed-dominance, fixed-population/support, Monte Carlo, P1–P3, bootstrap, sensitivity, or classification contracts.

## Corrections

- **R08:** N3 phase bins increase counterclockwise; the frozen clockwise search is therefore decreasing bin index with modulo-eight wraparound.
- **R09/R12:** every stochastic object gets a fresh, canonically keyed SHA-256 → unsigned-64-bit-big-endian → NumPy-2.3.5 PCG64 generator. Namespace serialization, object order, candidate order, and draw API are frozen.
- **R10:** phase and empirical-quantile bin boundaries are finite binary64 values with left-closed/right-open bins and exact-cutpoint assignment to the higher bin via right insertion.
- **R11:** N1 uses the forward map `original action a -> pi(a)`; all refit labels and physical outcome lookup are typed.
- **R13:** the accepted N5 contract is encoded reconstructively, including the explicit first twelve matrices, both tiers, all-path checks, minimum aggregation, and failure mapping.
- **R14:** sorted null values and the descriptive nearest-rank 97.5th percentile at one-based rank 195 are restored to machine metadata; the Monte Carlo p-value remains controlling.
- **R15:** the contract-only validator rejects the complete independent-review mutation registry and new A3 boundary mutations.

## Nonexecution

No rule in A3 authorizes registered seed release, fitting, null execution, sensitivity execution, bootstrap, outcome inspection, or scientific classification.
