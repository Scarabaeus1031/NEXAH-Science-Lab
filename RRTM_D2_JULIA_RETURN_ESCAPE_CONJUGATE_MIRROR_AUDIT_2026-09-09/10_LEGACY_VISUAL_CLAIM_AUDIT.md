# Legacy Visual Claim Audit

## ATLAS COMPLEXA

The image explicitly labels the Mandelbrot set “Parameter Space,” the Julia set “Behavior Space,” gives `c=-0.750+0.100i`, and states that `c` is not the Julia set but describes which Julia emerges. The parameter/dynamical-plane distinction is directionally correct standard complex dynamics: `SOURCE_DOCUMENTED`, `ESTABLISHED_COMPLEX_DYNAMICS`.

“Behavior Space” is conceptual wording rather than a formal variable definition. The five C’s—context, constraint, coupling, coherence, change—are visible conceptual labels; no implementation binding them to standard Julia variables was found: `SOURCE_LABEL_ONLY`.

## LUNAR MOON SONG V2

`44.4 kHz-44.1 kHz=0.3 kHz=300 Hz`: `EXACT_ARITHMETIC`.

The displayed 1.5 Hz beat envelope does not follow from those two frequencies without a separately defined transformation: the direct difference-frequency inference is `CONTRADICTED`.

The 15-year geodetic rotation, Earth–Moon orbit at 44.1 kHz, Earth-shadow orbit at 44.4 kHz, field interferometry, and tesseract state-space statements are present as labels but lack bound measurements, equations, units, calibration, and independent data: `SOURCE_LABEL_ONLY`, `UNSUPPORTED_PHYSICAL_CLAIM`.

## Quantum Flip visual

“Tachyon Flip,” “Proton Energy Stabilization,” “Pleiadian Light,” and “Klein Bottle Fold” are plotted labels. No exact generator or physical evidence was located: `SOURCE_LABEL_ONLY`, `UNSUPPORTED_PHYSICAL_CLAIM`.

## EXP-JANUS-19C

The visual documents a supervised prediction/ablation/AUC experiment with five functional axes. It is not a Julia iteration. Shared “Janus,” “pivot,” “portal,” “orientation,” and “return” language does not produce mathematical identity: `SOURCE_DOCUMENTED` for its own experiment and `INSUFFICIENT_SOURCE_BINDING` for a Julia relation.

## Intake `julia_c(1).png`

Visible labels bind `c=-0.750+0.100i`, axes spanning [-2,2], an escape-time quantity, and a 250-iteration bounded-at-horizon convention. No exact generator was located. Matrix resolution, sample convention, normalization, complete colormap contract, and render layout are not source-bound. The audit render is `QUALITATIVELY_SIMILAR`, but byte, pixel, and array comparison with this PNG is `NOT_COMPARABLE`.
