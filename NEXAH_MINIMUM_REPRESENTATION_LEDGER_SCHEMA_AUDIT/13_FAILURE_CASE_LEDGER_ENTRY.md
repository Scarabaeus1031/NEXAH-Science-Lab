# Failure-Case Ledger Entry

## TF-R3-DECODE: delayed representation → decoded transition certificate

The underlying edge remains documented; a preservation claim attached to it failed.

- Source/target: `ARRAY/time-series observations` → `STATISTICAL_SUMMARY/decoded transition certificates` through one-step delay representation, independent k-medoids decoding, alignment, and certificate extraction.
- Status: `PARTIALLY_VERIFIED` for execution and recorded outputs.
- Failed claim: “the exact v0.7 HIGH/LOW support versus LOW/HIGH weight hierarchy is decoder-independent.”
- Negative disposition: `NOT_REPLICATED`.
- Evidence: Study 2 support preservation `31/32`; Study 3 support preservation `0/20`, mean mapping accuracy `0.790`; Study-3 support overall `0.780/0.767` (`MEDIUM/MEDIUM`).
- Collision record: Study 3 reports dominant delay collision pairs in all `20` evaluated families under the registered alignment/certificate criterion; this is an observed decoder-level collision result, not an externally defined task loss.
- Consequence: delay labels do not guarantee decoded fidelity; the exact hierarchy is retired as decoder-independent.
- Reopening condition: genuinely external preregistered replication across independent decoders, fixtures, delay depths, and alignments; no internal retuning.

The ledger retains both the original claim and contradicting evidence. It does not mark the computational edge `FAILED` merely because one general interpretation failed, and it does not erase the negative result when the broader prior-art trade-off remains plausible.
