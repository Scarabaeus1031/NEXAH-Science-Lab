# Adversarial Self-Review

| Attack | Frozen answer |
|---|---|
| H1 by tolerance change | impossible: primary values/decisions exact; F3 fatal |
| NEXAH reads baseline Boolean | no; modules are separate and N4 accepts public cases only |
| correspondence reveals k* | aliases contain no value/truth; N2 ablation and F5 bar advantage attributable to correspondence alone |
| truth method-dependent | no; generator oracle computes exact target-value survival before diagnostics |
| controls mistaken for discoveries | impossible: suite flag excluded before endpoints; F9 fatal |
| one lucky case triggers H1 | impossible: >=9 unique correct plus aggregate margins and >=3 witness groups |
| failed fixtures disappear | no; fixed counts and F7 produce durable failure |
| NO_LOSS absent | 12 held-out plus controls; explicitly scored |
| prose absent in code | code implements E1–E8, F1–F10 and total precedence |
| scientific/machine H1 mismatch | same constants and scorer predicates define both |
| baseline can defeat NEXAH | **fatal YES**: an omitted task-aware exact sequential baseline is definitionally identical to N4 |

## Fatal conclusion

N4 sees the public task query, public correspondence and exact paired stage
values. Its algorithm is precisely the plug-in evaluation of the estimand's
survival predicate. An independently implemented conventional method with those
same fair inputs must return the same survival vector and `k*`.

The draft therefore has no non-circular route to incremental value:

- omit that comparator: N4 receives a task-specific capability denied to the
  baseline pool, creating an artificial positive;
- include it: N4 and the fair baseline are definitionally equal, so H1-A/B cannot
  be true.

This is not solved by more held-out cases, blinding, ablation, thresholds or code
separation. The estimand remains exact, but Q2 is trivial under equal information.

```text
FATAL_SELF_REVIEW_ANSWER = YES
PROTOCOL_FROZEN = NO
REVIEW_DECISION = DO_NOT_FREEZE_T02_V2
```
