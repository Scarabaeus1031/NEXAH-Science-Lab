# NEXAH Φ–Fourier–Entropy Source Forensics

Mode: BOUNDED_ADDITIVE_SOURCE_FORENSICS_AND_REPRODUCTION_TEST

Date: 2026-09-09

## Result

**DECISION = E — insufficient source binding; STOP.**

No immutable numerical array is bound as the shared upstream source of the named Fourier and “Φ-Entropy” visuals. The two Python candidates generate their own analytic 360-point spiral coordinates and do not read any supplied CSV. The phi variable is defined but unused in both scripts. Neither script calculates a Fourier transform, entropy, the Riemann zeta function, the Möbius arithmetic function, or κ_φπ.

The local archive copy was used as the frozen primary corpus. A Google Drive mirror was located but not consumed, avoiding cloud synchronization as an evidential dependency.

## Reproduction status

- zeta_mobius_overlay_spiral.py: one isolated execution attempt; stopped at import with ModuleNotFoundError: numpy under system Python. The bundled Python contains NumPy 2.3.5 but lacks Matplotlib. No installation was authorized. No successful generator execution occurred.
- zeta_prime_lock_mobius.py: not executed. Static inspection shows anim.save(...mp4), which invokes an unresolved external animation writer/process. Fail-closed stop applied.
- No supplied output was reproduced byte-exactly, pixel-equivalently, or numerically from a source generator.
- Independent baseline calculations were completed against the unbound 8×8 numerical table; these are audit calculations, not legacy reproduction.

## Main scientific disposition

- Fourier and Gaussian relations are established mathematics, not a new harmonic law.
- The “Φ-Entropy” image has no bound formula or input array and is therefore an unlabeled/mislabeled visual candidate.
- κ_φπ is a dimensionless number computed from φ and π; no tested role exists in the supplied generators.
- Numerical residuals produced by a controlled FFT round trip are at ordinary floating-point scale. The separate legacy 10^-15 display remains unbound.
- Zeta/Möbius visuals and scripts do not bind to the Fourier/entropy table or visuals.
- Several elementary sinusoidal and reciprocal-period equations survive as mathematics; physical interpretations and units generally do not.

All source artifacts remained unchanged.

