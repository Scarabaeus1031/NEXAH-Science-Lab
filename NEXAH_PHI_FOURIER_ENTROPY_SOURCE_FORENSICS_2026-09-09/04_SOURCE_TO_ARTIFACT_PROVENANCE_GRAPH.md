# Source-to-Artifact Provenance Graph

## Bound code graphs

### Static overlay

source code cf639b…
→ embedded parameters n=360, θ∈[0,4π], z∈[−2,2], r=1+0.3sin(5θ) — HASH_BOUND / INFERRED_FROM_CODE
→ internal arrays x,y,z and prime_indices — INFERRED_FROM_CODE
→ Matplotlib 3D scene — INFERRED_FROM_CODE
→ named PNG ZETA-@ SPIRAL OVERLAY • Final.png — TITLE_ONLY / UNRESOLVED

There is no save call, output path, metadata manifest, or image hash in the script. The supplied PNG cannot be hash-bound to it.

### Animated prime lock

source code c4ca96…
→ same internal analytic arrays — INFERRED_FROM_CODE
→ frame-dependent prime subset, 90 frames — INFERRED_FROM_CODE
→ zeta_prime_lock_mobius.mp4 — PATH_ONLY
→ existing MP4 bytes — UNRESOLVED

The source names the MP4, but reproduction was blocked before execution.

## Fourier/entropy graph sought

p(x,y) — UNRESOLVED
→ normalization and grid/cell measure — UNRESOLVED
→ FFT convention — UNRESOLVED
→ Re, Im, |P|, |P|², phase arrays — UNRESOLVED
→ Fourier Transform · kₓ–kᵧ Harmonic Space.png — TITLE_ONLY

p(x,y) — UNRESOLVED
→ −p ln p or −k_B p ln p — UNRESOLVED
→ local entropy array / integral — UNRESOLVED
→ Φ–Entropy Crown Projection · Boltzmann-Lattice Harmonic.png — TITLE_ONLY

The 8×8 Microwave_Background_Intensity_Map.csv is a possible numerical table but has no code, manifest, metadata, or hash reference connecting it to either visual. It is therefore UNBOUND_NUMERICAL_TABLE.

## Zeta/Möbius and table separation

The scripts consume no CSV. The resonance grids and Silver Rain catalyst table have no downstream code references. The Atrium 0.429 filename supplies only a title-level association. No arrow from these tables/visuals to Fourier or entropy reaches HASH_BOUND status.

## Reproduction classification

- Byte exact: not achieved.
- Pixel equivalent: not tested because no bound renderer/dependencies.
- Numerically equivalent: achieved only for independent FFT identities on the unbound 8×8 candidate, not for a supplied visual.
- Overall source binding: **E — insufficient; STOP**.

