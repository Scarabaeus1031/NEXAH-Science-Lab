# NEXAH Removal Test

`NEXAH_REMOVAL_TEST = PARTIAL`

An external laboratory can understand the broad schema without symbolic vocabulary: sources, targets, operators, evidence, provenance, loss, uncertainty, tasks, and negative results are ordinary scientific infrastructure concepts.

The current sample records do not fully pass because:

- they are prose, not portable conforming instances;
- repository-relative references assume local NEXAH context;
- historical labels such as “critical,” “basin,” and “transition graph” require the audit's decontamination narrative;
- Case D's aggregate hierarchy claim requires cross-study context not modeled as a first-class claim object;
- representation contracts and operator components are not self-contained.

With the minimum blocking revisions and portable locators/contracts, the concept should become name-independent. It is not there yet.

