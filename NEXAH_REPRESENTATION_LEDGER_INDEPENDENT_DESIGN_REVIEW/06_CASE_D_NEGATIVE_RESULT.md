# Case D — Negative Translation-Fidelity Result

## Evidence-derived reconstruction

The evidence supports two simultaneous statements:

1. the registered Study-3 pipeline executed reproducibly and produced its frozen records;
2. the claim that the exact v0.7 support/weight hierarchy is decoder-independent was `NOT_REPLICATED`.

The negative claim is aggregate evidence across many transformation/decoder/certificate edges, not a property of one simple `delay → decoded certificate` edge. The delay path itself is a composite of delay construction, independent k-medoids decoding, correspondence alignment, transition extraction, and certificate computation. Study 2 support preservation was `31/32`; Study 3 was `0/20`, with mean mapping accuracy about `0.790` and 20 reported dominant collision pairs.

## Review classifications

- Operator: `EDGE_TOO_COARSE` in the previous prose record.
- Collision: objective within the frozen decoder/alignment criterion; no external task loss follows.
- Negative result: evidence-derived and correctly retained.
- Provenance: `COMPLETE_FOR_CLAIM` at study/synthesis level; component-edge record identities are absent.
- Status: execution verified and scientific hierarchy rejected must be separate dimensions.
- Existing-record agreement: `INTERPRETIVE_DIFFERENCE`. The conclusion agrees, but attachment to one synthetic edge rather than a first-class cross-edge claim is underdetermined.

The schema can store a negative array beside a positive status, but cannot prevent a later record from dropping that array. Representation is possible; safe retention is not.

