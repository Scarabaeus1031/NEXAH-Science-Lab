# Final Recommendation

## Result

`PROTOCOL DESIGN COMPLETE`

The smallest complete protocol is a deterministic finite classification over four source identities, 121 matched samples, and four fixed cardinal projection matrices.

## Frozen protocol decision

- finite sampled scope;
- canonical input table with 484 rows;
- four integer observation matrices;
- maximum-norm discrepancy over full displayed records;
- scientific tolerance `1×10^-9`;
- numeric replay tolerance `1×10^-12`;
- exact categorical replay agreement;
- one partition per view;
- positive, negative, inconclusive, or invalid-protocol result;
- independent implementation and bounded external review only after separate authorization.

## Every remaining undefined scientific or operational quantity

| Undefined item | Required resolution before execution |
|---|---|
| canonical source table | create and freeze the 484-row CSV from the existing 121-sample source family |
| coordinate semantics | declare units or explicitly declare all coordinates dimensionless |
| decimal precision | declare canonical text precision and source-export rounding provenance |
| source snapshot | identify exact originating files and SHA-256 hashes |
| export process | document one export method and independent export review |
| protocol adoption | record adoption date, version, and Thomas's signature |
| scientific owner | name accountable inverse-problems owner |
| freeze owner | name custodian of immutable package and hashes |
| execution owner | name independent implementation owner |
| validation reviewer | name reviewer independent of implementation |
| protocol reviewer | approve definitions before any implementation |
| repository/output boundary | name permitted read and write locations for a later run |
| implementation | create independently under later authority |
| numeric environment | declare numeric representation, language, dependencies, and lock data |
| independent replay group | select and document independence |
| external reviewers | select profiles and document conflicts |
| release authority | record who may transmit packages externally |
| clarification process | establish versioned, equally distributed protocol clarifications |

No undefined scientific formula, comparison, tolerance, result class, or STOP rule remains in Protocol 1.0.

## Objects that must be frozen before execution

- owner and authority record;
- all twelve Program G documents;
- canonical input CSV and dictionary;
- scientific-object record;
- matrices, pair order, norm, and tolerances;
- equivalence and partition rules;
- input and output schemas;
- validation checklist and decision table;
- provenance and non-claims;
- independent replay archive specification;
- file inventory and SHA-256 hashes;
- permitted repository and output boundary.

## Assumptions that must never change during replay

- four persistent source labels;
- 121 index-matched samples per source;
- exact input bytes;
- four fixed integer matrices;
- displayed coordinates are the only observation;
- no masks, depth, preprocessing, interpolation, or reconstruction;
- maximum norm over all samples;
- tolerance values and `≤` boundary rule;
- exact pair, partition, null, and terminal classifications;
- evidence precedes interpretation;
- finite-only claim boundary;
- failed runs and disagreements remain preserved.

## Consolidated STOP conditions

Stop if authority, ownership, source freeze, hashes, units, precision, schema, matrices, tolerance, validation independence, or output boundary is absent.

Stop on any input mismatch, missing or non-finite value, unauthorized transformation, threshold drift, identity loss, incomplete comparison, non-transitive relation, invalid partition, evidence/interpretation inversion, result-rule ambiguity, replay leakage, categorical disagreement without classification, or scope expansion.

Stop after one terminal result. No new view, continuous claim, reconstruction, application, publication, or successor protocol opens automatically.

## Next permitted action

Owner review of Program G.

If accepted, the next permitted action is documentation and governance only:

1. name the scientific, freeze, protocol-review, execution, and validation owners;
2. create the canonical input and replay packages without running the protocol;
3. hash and review the packages;
4. return to authority for a separate execution decision.

No implementation or execution is authorized.

## Final record

```text
RESULT: COMPLETE
PROGRAM_STATUS: PROTOCOL DESIGN COMPLETE
PROTOCOL: Finite Identifiability Classification Under Four Orthographic Projections 1.0
SCIENTIFIC_SCOPE: Four source identities; 121 matched samples; four fixed orthographic observation maps
PRIMARY_OUTPUT: Four view-specific observational-equivalence partitions
SCIENTIFIC_TOLERANCE: 1e-9
REPLAY_TOLERANCE: 1e-12
OUTCOMES: positive; negative; inconclusive; invalid protocol
EXECUTION_READY: NO
CRITICAL_BLOCKER: Canonical input package, source hashes, authority, owners, and independent validation roles are not frozen
NEXT_PERMITTED_ACTION: Owner review and non-executing package freeze
OPERATIONAL_EFFECT: NONE
```
