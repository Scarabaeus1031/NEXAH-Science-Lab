# REE-G9-D1 Repair

## REE-G9-D1-R1

Registered evidence generation and all scientific export calculations use only:

```text
Python 3.12.13
NumPy 2.3.5
macOS 26.5.2 arm64
IEEE-754 binary64
```

This is the explicit V1/A3/A4/A5XEF authority, not a new runtime choice. Any other environment fails before registered seed resolution.

The observed authority-compatible bundled artifact is identified by `REGISTERED_RUNTIME_AUTHORITY.json`. Its Python executable, NumPy distribution tree, NumPy module/core/linalg bytes, platform and Accelerate linkage must verify before generation and remain the consumed runtime through scientific export. Material callable substitution, preloaded alternate NumPy, path shadowing or artifact drift fails closed.

The historical runtime `ANACONDA_PY3127_NUMPY1264_OPENBLAS0321_ARM64` remains valid only for reproducing sealed historical synthetic reference objects and for the authority role already assigned by V3R5/V3R6. It is prohibited for registered Rössler state generation, target/support computation, representation fits, outcomes, N5-RUN evidence, sensitivities and registered scientific-export numerical values.

JSON key ordering, separators, UTF-8 and atomic write remain the prior operational serialization contract. Float rendering must occur under the registered 3.12.13 runtime for the registered object.

