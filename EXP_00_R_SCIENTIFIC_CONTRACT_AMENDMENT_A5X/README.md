# EXP-00-R Scientific Contract Amendment A5X

A5X is an additive contract-engineering repair for A5-R26–R29. It changes no scientific rule and creates no implementation or authorization. Raw synthetic artifacts are used only for conformance testing.

Run `python3 contract_validation/validate_a5x_contract.py` and `python3 -m unittest contract_validation/test_a5x_contract.py contract_validation/test_a5x_attacks.py` from this directory.

