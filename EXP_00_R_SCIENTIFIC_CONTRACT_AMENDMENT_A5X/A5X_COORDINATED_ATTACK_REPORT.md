# A5X Coordinated Attack Report

Independent contract-only tests cover A–X. All attacks are required to reject: coordinated root+prose, root+machine, root+validator; V1 source/config; root count/role/algorithm; parity/leakage/isolation/distinctness; N2/N3; RNG serialization/order; sensitivity ID/path/value/unchanged config; P5 coefficient/gain; classifier second branch; P4 choice; Lorenz ceiling.

**Result: PASS.** Every material mutation is rejected by the trusted unchanged validator or by raw-artifact predicate evaluation. No registered science is imported or run.

