# Suggested Commits

Each commit is intentionally small. No commit changes scientific authority by implication.

## Commit 1 — Add Mission 01 report only

```text
docs: add scientific backbone distillation report
```

Scope: the report directory only. No canonical files changed.

Review gate: verify source paths, workspace provenance, and the absence of `00_INCOMING/`.

## Commit 2 — Add operator vocabulary boundary

```text
docs: distinguish OLS primitives from Library operator concepts
```

Scope: Library README and Library Architecture only.

Review gate: Library owner confirms that IDs, classifications, and Work relations remain unchanged.

## Commit 3 — Add current operator index

```text
docs: add authority-aware operator index
```

Scope: one index and Repository Map link.

Review gate: every entry has one authority, semantic kind, scope, maturity, and source link.

## Commit 4 — Freeze Gate terminology

```text
docs: name gate score as local instability measure
```

Scope: current Architecture and Demonstrator entry documents only.

Review gate: historical logs stay unchanged; current text does not claim transition detection or probability.

## Commit 5 — Freeze JANUS identities

```text
docs: separate JANUS principle bridge and coherence measure
```

Scope: one identity page and links from current Architecture/Research indexes.

Review gate: no historical file is rewritten; Atlas `J` receives an explicit crosswalk.

## Commit 6 — Mark Atlas chain historical/conceptual

```text
docs: reclassify atlas operator chain as conceptual pipeline
```

Scope: banner and forward links in the Atlas Operator Framework document.

Review gate: experimental claims and figures remain intact; no new OLS relation is asserted.

## Commit 7 — Draft typed transition record

```text
research: draft evidence-bound typed transition record
```

Scope: non-normative schema, examples, and validation tests.

Review gate: graph, IEEE, cluster, and continuation examples preserve incompatible semantics and return insufficiency where required.

## Commit 8 — Draft path and boundary records

```text
research: define candidate path and boundary records
```

Scope: candidate contracts and tests only.

Review gate: no universal topology, manifold, physical boundary, or causality claim.

## Commit 9 — Freeze Gate experiment protocol

```text
research: preregister gate instability validation
```

Scope: protocol, dataset split, baselines, metrics, and stop criteria. No result claims.

Review gate: held-out evaluation and ablation baselines cannot be changed after preregistration without a recorded amendment.

## Commit 10 — Freeze DCO experiment protocol

```text
research: preregister directional coherence discrimination test
```

Scope: one formula, insufficiency rules, baselines, and hypotheses.

Review gate: distinguishes DCO from direction change, curvature, reversal, and inverse transitions.

## Commit 11 — Add domain reader-test protocols

```text
research: preregister biology and chemistry orientation tests
```

Scope: protocols only.

Review gate: correctness, boundary recognition, time, and preference remain separate outcomes.

## Commit 12 — Add historical crosswalk

```text
docs: map historical operator labels to current categories
```

Scope: one generated or reviewed crosswalk; no source deletion.

Review gate: every `OBSOLETE` item retains provenance and a current destination or explicit “no current equivalent.”

## Sequence rule

Do not combine commits 2–6. Each changes a different authority boundary and should be independently reviewable and revertible.
