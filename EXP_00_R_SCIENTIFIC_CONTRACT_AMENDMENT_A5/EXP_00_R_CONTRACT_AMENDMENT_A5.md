# EXP-00-R Contract Amendment A5

**Status:** prospective scientific contract; not implementation; not authorization; not execution.  
**Authority:** frozen V1 composite `971d4d947366f54692f72bbf20ac89ac4fcf7d11d4654f65bd4bfc6e1658bc05`, then accepted A1–A4, then A5.

A5 changes exactly one scientific rule: it prospectively makes the mandatory TRAJECTORY-only and LEARNED_FIELD-only outcome-prediction analyses report-only diagnostics rather than P4 vetoes. This is labeled **NEW PROSPECTIVE SCIENTIFIC CHOICE**. It is selected because single-view predictors have no pairwise coherence estimand and V1 explicitly types them as diagnostics; it is not selected for passability. Their execution and reporting remain mandatory, so missing outputs invalidate the experiment.

P4 remains classification-critical for both symmetric carrier coherence analyses. Each carrier requires positive standardized coherence coefficient, positive held-out baseline-minus-augmented log-loss gain, both representations' score/margin controls, at least 21/30 nonnegative per-seed directions, and exact signed seed-dominance PASS. The two single-view predictive diagnostics and equal-score fusion cannot pass, fail, rescue, or veto P4.

A5 additionally makes all validity gates executable, types all 12 sensitivity outputs, restores A2 exact-rational seed dominance without a float tolerance, and seals authority/prose/machine/tests. N1–N5, P1–P3, P5, classification precedence, and the Lorenz-v2 ceiling retain A4 semantics under immutable bindings.

Any missing, malformed, nonfinite, inconsistent, or provenance-mismatched mandatory artifact makes the registered experiment `INVALID EXPERIMENT`; before authorization it makes authorization unreachable. No partial completion or post-access repair is permitted.

