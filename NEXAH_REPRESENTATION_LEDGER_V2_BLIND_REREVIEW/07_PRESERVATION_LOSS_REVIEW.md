# Preservation/Loss Review

V2's mandatory criterion, result, evidence, and judgment fields are a large improvement. A and B use deterministic property/output-contract criteria that an independent reviewer can reproduce. D uses frozen empirical comparison criteria and does not infer task harm.

The categorical separation works:

- A/B structural output reduction;
- B potential missed border contacts;
- D observed-instance decoded identity loss;
- no task-relevant loss because all tasks are `UNDEFINED`;
- unknown historical collision in A remains unknown.

The remaining problem is assertion applicability. In C, approximate recovery belongs to rotation/inverse components, while the declared edge ends in a record. Its loss claim belongs to an unstated subsequent projection. V2 requires a criterion but does not require the claim to identify whether it applies to the entire edge, a component list, or a derived projection. A formally operational criterion can therefore still answer the wrong transformation question.

No criterion in A/B/D merely restates its conclusion. Scientific truth still requires source review; schema form alone cannot verify it.

