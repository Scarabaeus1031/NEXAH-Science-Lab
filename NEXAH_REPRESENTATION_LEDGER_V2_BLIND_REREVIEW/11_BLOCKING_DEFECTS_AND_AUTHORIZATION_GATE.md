# Blocking Defects and Authorization Gate

Three small, precise blockers remain. They justify the single allowed bounded revision, not another open-ended schema cycle.

| # | Blocker | Required bounded V3 repair |
|---:|---|---|
| 1 | assertions/invertibility lack whole-edge versus component/subpath scope; C consequently misbinds source artifact and two claims | require `applies_to: EDGE | COMPONENTS | DERIVED_PROJECTION`, referenced component IDs/projection contract, and correct C source artifact |
| 2 | controlled top-level epistemic values can contradict unconstrained duplicated detail `value` strings | remove duplicate values or constrain each detail value to the corresponding dimension enum and equality rule |
| 3 | append preservation can be bypassed by resetting record/claim lineage | require stable history-series ID, prior-record reference for later revisions, unique claim IDs, and cross-record retention derived from the referenced prior record |

Gate results:

- Cases are predominantly substantive, but C has a V2-created evidence conflict.
- Representation typing, actual serialization, judgment labels, missing provenance, and syntax/science separation pass.
- Preservation/loss and epistemic/history repairs remain partial.
- `MINIMAL_VALIDATOR_SPECIFICATION_AUTHORIZED = NO` until a bounded V3 and final review.

No further revision beyond that single bounded V3 should be used to rescue the project. Failure there reduces the ledger to structured documentation/provenance.

