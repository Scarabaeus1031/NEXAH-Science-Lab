# Seven-Repair Verification

| Repair | Verdict | Finding |
|---|---|---|
| edge/operator boundaries | `PARTIALLY_RESOLVED` | explicit/composite/unresolved boundaries work, but claims cannot bind to a component/subpath |
| representation typing | `RESOLVED` | minimal contracts and case-relevant qualifiers distinguish all current endpoints |
| preservation criteria | `PARTIALLY_RESOLVED` | criteria/results are mandatory and useful, but Case C criterion applicability is not bound to the relevant component |
| loss criteria | `PARTIALLY_RESOLVED` | categories/criteria distinguish structural, observed, potential, task, unknown; Case C records loss from an undeclared later projection |
| overloaded verification | `PARTIALLY_RESOLVED` | five top-level dimensions are separate, but duplicated `dimensions.*.value` strings are unconstrained and may contradict them |
| negative-result history | `PARTIALLY_RESOLVED` | history is expressible and documented append-preserving, but a later author can reset series identity or omit prior claim IDs |
| real serialized records | `RESOLVED` | four YAML instances exist, conform to V2, and point to source evidence |

Renaming alone was not credited. All seven areas improved materially; two are fully resolved, while five retain scoped deficiencies concentrated in three blocking mechanisms.
