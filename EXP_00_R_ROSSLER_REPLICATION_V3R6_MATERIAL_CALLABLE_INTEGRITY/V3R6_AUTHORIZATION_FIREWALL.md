# V3R6 Authorization Firewall

Production authorization remains `NOT_GRANTED`. The registered entry point completes transitive authority, V3R5 historical runtime binding, V3R3 snapshot construction and V3R6 callable binding before checking authorization. It requires `GRANTED` before touching its input argument.

Instrumented unauthorized attempt:

```text
UNAUTHORIZED INPUT RESOLVE COUNT: 0
UNAUTHORIZED INPUT READ COUNT: 0
```

No authorization fixture or production authorization was created. No registered path was resolved or read.
