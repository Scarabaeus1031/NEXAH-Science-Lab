# Final Independent Adversarial Review — A5X

## Decision

**REJECT A5X — CLASS B DEFECT.** The scientific contract remains closed and A5X's finite authority-root design is honest and effective. The encoding fixpoint is not reached because raw artifacts do not mechanically derive several mandatory scientific objects.

## Blocking findings

| ID | Class | Finding | Classification consequence |
|---|---|---|---|
| A5X-R30 | B | `P1_P5_MECHANICALLY_COMPUTED` accepts five producer-supplied Booleans plus `derived:true`; no raw P1–P5 derivation exists. | Arbitrary proposition truth values can enter the classifier. |
| A5X-R31 | B | Per-seed completeness omits exact seed dominance; N5/null gates validate counts/finiteness but not transformations, matrices, required statistics or provenance. | Invalid scientific worlds can pass validity. |
| A5X-R32 | B | Support accepts 20 arbitrary keys rather than registered test seeds; provenance expected IDs and sensitivity bindings are caller-supplied; sensitivity support accepts impossible ranges/counts. | Population/provenance/completeness can diverge across implementations. |
| A5X-R33 | B | Semantic validator omits accepted N1, N4, N5, RNG, action-order, P4 report-only and P5 variant/interval fields. In-memory semantic mutations pass. | Prose/machine/validator equivalence is incomplete. |

No Class-A defect or scientific change was found. No repair is made here.

