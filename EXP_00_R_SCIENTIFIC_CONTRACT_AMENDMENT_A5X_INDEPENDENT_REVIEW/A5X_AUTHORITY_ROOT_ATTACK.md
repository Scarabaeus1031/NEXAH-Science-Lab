# A5X Authority-Root Attack

Independent recomputation confirms root digest `c5b35bbbbc7551fb5cddd8cc5468ba0aa8bcf4e895ef8bbe0b55f341821c4100`, V1 composite, all 32 members and normalized validator hash.

Using the trusted original validator/root digest, isolated or coordinated root+prose, root+machine, root+validator, root+test, metadata/count/role/algorithm, omission, V1 source/config, A5 science and A5X machine byte mutations are rejected because any root rewrite no longer matches the hard-bound digest.

The stated finite trust boundary is honest: replacing the entire directory and the externally trusted verifier/digest is outside local self-verification. The review does not demand metaphysical immutability. Unlisted files are nonauthoritative; listed Markdown membership is scanned.

**Authority root/trust boundary: PASS.** This does not validate the semantics of the bytes it seals.

