# Final integration decision

PRIMARY_DECISION = **A - SHARED_OBSERVER_PROJECTION_LAYER_CONFIRMED**

GHOSTGRID_ROLE = **SHARED_LAYER**
DEMONSTRATOR_COUNT = **REMAINS_THREE**
RECOMMENDED_PROTOTYPE = **REVERSAL_COMPLEMENT**

Ghostgrid can provide a common typed projection/comparison envelope for Julia, CRT, and reversal/complement without changing any mathematical kernel. It does not merge their state spaces and is not an implementation.

Reversal/complement is recommended first because it has minimal dependencies, exact fixed-width states, immediate interaction, a visible invariant, explicit involution/reconstruction, and a sub-two-minute explanation. Julia and CRT remain the other two frozen demonstrators.

ILAU has an explicit four-class definition, but no existing Ghostgrid binding. The proposed relationship is a compatible observation-view/return-comparator extension, not a core change. QRT was not found and remains unresolved.

The layer overlaps existing projection and observer concepts but does not duplicate their authority: THE EYE/NEXAHEDRON may display; Observer Return/ILAU may classify comparison records; Representation Ledger may preserve provenance; ORION remains the kernel processor; OLS retains semantic authority; Human Authority decides.

No implementation, source modification, prior-workstream reopening, ORION change, commit, or push.
