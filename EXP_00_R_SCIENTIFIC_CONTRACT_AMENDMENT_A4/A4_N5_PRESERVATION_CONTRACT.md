# A4 N5 Preservation Contract

The independent A3 review accepted N5. A4 preserves it without scientific change.

- N5 is deterministic coordinate-equivariance integrity/validity, not a stochastic null and not P1–P3 evidence.
- Use all determinant-`+1` 3×3 signed permutations, sort row-major nine-integer tuples with `-1<0<1`, and select the exact first 12 matrices listed in the machine contract, once per tier in that order.
- Transform every coordinate, training-path state, query, `B`, standardizer, and target consistently; scalar actions/radius remain; no target-subset reselection. Refit support and both representations with V1 hyperparameters and support quantile `.99`. Inverse-register every state-valued terminal prediction with `Qᵀ` before original-target scoring.
- N5-SYNTH uses the accepted fixed linear field, grids, action-conditioned paths, target, and at least 20 original jointly supported synthetic queries before authorization/seed release. Failure is `IMPLEMENTATION_FAILURE` and authorization remains forbidden.
- N5-RUN follows authorization, registered training/target/original fits/predictions/support accounting, and precedes outcomes, regression, N1–N4, sensitivities, bootstrap, or classification. Its population is original jointly supported held-out rows including zero-action carriers, frozen before transformed fits. Transformed abstention/refit error/population change is `INVALID_EXPERIMENT`.
- Compare five-action weak ranks using Kendall tau-b per tier/matrix/representation/query. Both constant-identical is 1; other undefined is 0; missing is failure. Each tier aggregates by the minimum over every comparison and passes iff minimum `>=.99`.

The machine artifact is the complete accepted encoding, including the 12 matrices and synthetic fixture.

