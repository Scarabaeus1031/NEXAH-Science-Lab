# A5 P4 Attribution Audit

Independent reconstruction before accepting A5's conclusion found:

- V1 P4 says direction and gain must be positive for every preregistered carrier/leave-one attribution after score/margin controls.
- V1 preregisters T- and F-carrier agreement-conditioned models and separately requires TRAJECTORY-only, LEARNED_FIELD-only, and fusion diagnostics.
- With two representations, removing one leaves no pair and therefore no Kendall `tau_b` or `C_tau`. Outcome prediction, carrier outcomes, log loss, AUC, Brier, calibration, score and margin remain mathematically defined.

A5 explicitly labels one **NEW PROSPECTIVE SCIENTIFIC CHOICE**: the two single-view predictors are mandatory report-only, while T/F carrier coherence analyses are P4-critical. Missing diagnostics invalidate through provenance, but their values cannot veto or rescue P4. No singleton coherence, oracle, new threshold, or new null is invented.

The Boolean P4 is local and exact: both carrier coefficient/gain checks, score/margin controls, both 21/30 direction checks, and both exact dominance checks. The classifier consumes this P4 through A4's frozen precedence. No unrelated frozen scientific rule is weakened.

**A4-R21: PASS. No Class-A defect.**

