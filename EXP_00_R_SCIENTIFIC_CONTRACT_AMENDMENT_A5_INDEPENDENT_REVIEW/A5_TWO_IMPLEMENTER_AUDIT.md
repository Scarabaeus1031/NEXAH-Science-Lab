# A5 Two-Independent-Implementers Audit

For scientific populations, representations, N1–N5, RNG draws, bins, carrier outcomes, P1–P5, seed dominance, and the Lorenz ceiling, the full authority stack supplies one intended construction.

The acceptance question nevertheless fails because the A5 closure requires each team to derive classification-blocking validity and sensitivity completion from future artifacts. Team X can interpret the uppercase `all` entries as precomputed trusted Booleans; Team Y can recompute them from raw records. They can disagree about source-manifest identity, provenance ledger membership, event-stage semantics, unchanged-config-leaf hashing, sensitivity identity, and therefore validity. Both can point to A5 because no executable artifact schema/evaluator resolves the difference.

The unanchored manifest further permits two byte-distinct A5 stacks to validate after coordinated updates. Thus the teams are not guaranteed the same authoritative contract, validity truth values, or final label.

**TWO-INDEPENDENT-IMPLEMENTERS TEST: FAIL.** The counterexample is classification-relevant and Class B.

