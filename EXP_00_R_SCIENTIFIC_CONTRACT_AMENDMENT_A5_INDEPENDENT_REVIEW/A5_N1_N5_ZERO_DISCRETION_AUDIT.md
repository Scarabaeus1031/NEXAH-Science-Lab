# A5 N1–N5 Zero-Discretion Audit

A5 hash-binds the accepted A4 machine fields for `ordering`, `rng`, `binning`, `shared_null`, `nulls`, and `N5`. Reading the authoritative stack reconstructs:

- **N1:** forward five-action permutation per representation/training-seed/replicate; both representations refit; original support fixed; null top physical action selects the existing all-action outcome; inverse lookup forbidden.
- **N2:** within split and seed, FIELD weak-rank vectors permuted without replacement; recipient `i` gets donor `p[i]`; carriers/outcomes/support/baseline fixed.
- **N3:** one different-seed donor with replacement per recipient from same mapped phase/target bin; bins increase counterclockwise and clockwise search decreases modulo eight; canonical donor order; no donor invalidates.
- **N4:** full-training leave-self-out distance cutpoints, exact right-insertion equality, higher-decile-first merge, carrier-specific `N4_T/N4_F`, FIELD ranks permuted within stored strata, outcomes fixed.
- **N5:** distinct SYNTH and RUN tiers; exact first 12 ordered determinant-`+1` signed permutations; state, paths, actuator `B`, mean, scales and target transformed; refit both representations/support; inverse-register before target scoring; fixed joint population; minimum Kendall aggregation; threshold `.99`; transformed abstention/refit/population change invalidates.

N1–N4 each require replicate IDs `0..199`; N4 contains two carrier subworlds under each common ID, not extra null repetitions. The authoritative scientific definitions remain zero-discretion. A5's validator does not enforce all of them, which is recorded under integrity rather than reopening their science.

**N1–N5 scientific discretion: PASS. N5 contract: PASS.**

