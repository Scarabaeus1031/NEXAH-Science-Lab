# A5 P1–P5 and Classification Audit

Independent reconstruction yields:

- **P1:** mean coherence and top-action agreement pass all N1, N2, N3, N4_T and N4_F comparisons with adverse ties and `k<=4`.
- **P2:** both carriers have standardized coefficient `>0` and 500-seed-cluster bootstrap lower endpoint `>0`; coefficient-null p-values are diagnostics.
- **P3:** both carrier gains `>0`, augmented Brier `<=` baseline, and `k<=4` against N1/N2/N3 plus matching N4 carrier world.
- **P4:** A5's two carrier coefficient/gain checks, four score/margin controls, both 21/30 directions, and both exact dominance passes; single-view/fusion numeric values report-only.
- **P5:** both amplitude variants `.25,1.0` retain coefficient and gain `>0` for both carriers; no nonprimary interval requirement and no rescue.

Exhaustive enumeration of validity, five propositions, T/F positive cores and resolved-negative status gives one label per state: invalidity first; all propositions plus both cores/no negative gives `REPLICATED`; at least one core/no negative gives `PARTIALLY REPLICATED`; otherwise `NOT REPLICATED`. The `if/elif/else` structure is mutually exclusive and exhaustive. A contradictory positive-core/resolved-negative state is a validity failure.

The cross-system function caps every positive valid Rössler state at `PARTIAL CROSS-SYSTEM REPLICATION`; strict cross-system replication is unreachable because frozen Lorenz v2 P4 is false.

**P1–P5 contract, classifier exclusivity/exhaustiveness, and Lorenz ceiling: PASS.** The A5 validator fails to protect several branches against coordinated manifest rewrites, addressed separately.

