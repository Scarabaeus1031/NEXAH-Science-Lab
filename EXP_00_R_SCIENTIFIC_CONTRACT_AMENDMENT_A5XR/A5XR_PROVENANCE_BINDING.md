# A5XR Provenance Binding

Expected provenance is constructed by the validator from artifact type, synthetic stage, immutable V1 composite, accepted A5X root, exact identity, and frozen config SHA-256. Producers cannot supply the expected side of a comparison. Support, null, N5 and dominance records are compared to constructed exact objects; sensitivity binding is compared to one independently fixed authority tuple.

