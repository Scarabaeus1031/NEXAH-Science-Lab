# A5XR Raw Artifact Schema

The bundle contains authoritative synthetic registries; support row blocks; raw observed statistics and 500 bootstrap coefficients; five null-family metadata/statistic columns encoding exactly 200 values; both N5 tiers and 12 matrices/results; raw per-row loss pairs; raw per-seed directions; 12 sensitivity identities/config hashes/results/support; and fixed provenance bindings.

Authoritative `valid`, gate, P1–P5, dominance-pass, N5-pass or classification fields are forbidden. Optional `NONAUTHORITATIVE_CACHE` values must exactly equal recomputation or fail closed.

