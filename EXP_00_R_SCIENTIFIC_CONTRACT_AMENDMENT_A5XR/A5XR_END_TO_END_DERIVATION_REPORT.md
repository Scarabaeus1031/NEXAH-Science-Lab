# A5XR End-to-End Derivation Report

Canonical synthetic bundle → identity/provenance → support → nulls → N5 → exact dominance → sensitivities → P1–P5 → validity → classifier executes without producer decisions.

Derived fixture result: support/null/N5/dominance/sensitivity valid; P1–P5 all true; synthetic Rössler label `REPLICATED`; cross-system label capped at `PARTIAL CROSS-SYSTEM REPLICATION`. These are fixture conformance assertions, not registered scientific results.

