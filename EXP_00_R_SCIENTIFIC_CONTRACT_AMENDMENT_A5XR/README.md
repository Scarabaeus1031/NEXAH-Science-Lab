# A5XR — Raw Derivation Closure

Additive encoding-only repair of A5X-R30–R33. It introduces no scientific rule, implementation, authorization or registered execution. The canonical bundle is synthetic and uses only `SYNTH_*` identities.

Run from `contract_validation/`: `python3 validate_a5xr_contract.py` and `python3 -m unittest -v test_a5xr_contract.py test_raw_derivation.py test_a5xr_attacks.py`.

