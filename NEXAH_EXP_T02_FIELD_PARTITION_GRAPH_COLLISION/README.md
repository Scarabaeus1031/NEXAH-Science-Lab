# NEXAH EXP-T02 — Field → Partition → Graph Collision Audit

`FROZEN / NOT EXECUTED`

This package preregisters three deterministic synthetic counterfactuals for a
stage-wise information-loss audit. It is designed so the complete conventional
baseline set can defeat the proposed NEXAH collision ledger.

Start with `00_SCOPE_AND_BOUNDARIES.md`, then read `03_HYPOTHESES.md`,
`05_COUNTERFACTUAL_REGISTRY.md`, `08_BASELINE_REGISTRY.md`,
`12_FALSIFICATION_RULES.md`, and `13_DECISION_RULES.md`. `run_exp_t02.py` is
prepared but has not been executed; without both explicit execution flags it
exits without creating results. Any protocol or runner change invalidates the
manifest and requires a new preregistration version before execution.

## Frozen unresolved feasibility conditions

Because execution is prohibited at preregistration, it remains unobserved whether
the frozen detector finds exactly the declared maxima and whether B/C meet their
partition/graph construction conditions on all controls. These are not adjustable
design choices: failure maps mechanically to `PRECONDITION_FAILED`. No hidden
fixture, threshold, graph rule or replacement counterfactual remains.

## Frozen status

```text
PROTOCOL_DRAFTED = YES
PROTOCOL_FROZEN = YES
EXPERIMENT_EXECUTED = NO
CANONICAL_NEXAH_CHANGED = NO
LEGACY_PNG_USED_AS_RESULT = NO
SOURCE_FIELD_DETERMINISTIC = YES
SOURCE_TARGET_CORRESPONDENCE_PLANNED = YES
COUNTERFACTUALS_PREREGISTERED = YES
BASELINES_PREREGISTERED = YES
COLLISION_LEDGER_PREREGISTERED = YES
INCREMENTAL_VALUE_NULL_ALLOWED = YES
POST_RESULT_RETUNING = NO
NEW_MATHEMATICAL_CLAIM = NO
PHYSICAL_CLAIM = NO
NEXT_ACTION = REVIEW_FROZEN_T02_PROTOCOL_BEFORE_EXECUTION
```
