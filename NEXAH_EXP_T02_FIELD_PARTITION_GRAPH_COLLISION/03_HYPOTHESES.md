# Hypotheses

## H0 — primary null

The collision-aware, stage-wise NEXAH audit supplies no incremental diagnostic
information beyond the registered conventional baselines. Explicit collision
records and stage-wise R/D reporting identify no relevant loss event more
accurately, earlier, or more decision-usefully than those baselines.

## H1 — bounded alternative

For at least one registered counterfactual class, the collision/correspondence
audit identifies a preregistered task-relevant representation failure that the
complete registered baseline set misses or materially obscures.

H1 is supported only under `13_DECISION_RULES.md`. It does not imply a new
invariant, law, physical result, prediction, control capability, information-
theoretic optimum or generalization outside these three synthetic pairs.

## Exact fixture expectations

- `CF_A_AMPLITUDE`: C0 and C1 change; C2–C6 preserve.
- `CF_B_GEOMETRY_GRAPH_COLLISION`: C0–C4 change; C5 and C6 preserve. Equality at
  C5 while metric geometry/partition are task-declared different is the planned
  collision.
- `CF_C_ADJACENCY_CHANGE`: C0–C5 change; C6 preserves because both graphs are
  expected connected.

Prospective collision targets are exactly B:C5, B:C6 and C:C6. Equality at
other certificates registered `PRESERVE` is intended invariance for that
certificate's task and is not relabeled a collision. No collision is targeted
for A because amplitude is deliberately outside C2–C6.

Failure of the construction preconditions is not evidence for H0 or H1; it is
`PRECONDITION_FAILED`.
