# A5XE Two-Implementer Audit

The second derivation does not import or wrap the reference derivation and independently implements fitting, null transformations, bootstrap, N5, dominance, sensitivities and classification. It does share the synthetic raw module's registries, digest and transform registry.

On the canonical bundle, complete normalized records are exactly equal. On summary/direct-decision/N5-invalid mutations, both reject with the same typed state.

However, equality is not contract conformance. On the refreshed semantic counterexample, both paths reproduce the same erroneous positive record. Both hard-code the same omissions: all-row populations, ignored model specification, absent P4 control enforcement, and under-validated sensitivities.

Therefore:

- implementation-to-implementation equality: **PASS**;
- controlling accepted-contract two-implementer test: **FAIL**.
