# A5XE Final Decision

**REJECT A5XE — CLASS B DEFECT.**

Smallest blocking counterexample: a provenance-refreshed canonical synthetic bundle with valid 93.33% joint support, but with 100 unsupported TEST rows still entering primary/null models, missing P4 score/margin controls, empty mandatory diagnostics, an unregistered ignored model specification and contradictory amplitude-sensitivity metadata. Both derivations still return `VALID_SCIENTIFIC_RESULT / REPLICATED / P1–P5=true`.

Further blockers are incomplete semantic machine validation and an authority ledger that omits A1.

No V3 implementation review should begin from A5XE as the evidence/encoding fixpoint. This rejection creates no authorization and uses no registered data.
