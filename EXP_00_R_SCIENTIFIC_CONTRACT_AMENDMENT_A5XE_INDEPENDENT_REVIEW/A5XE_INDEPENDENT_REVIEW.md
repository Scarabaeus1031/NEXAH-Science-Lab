# Final Independent Adversarial Review of A5XE

This review treats A5XE as immutable and uses synthetic evidence only. It creates no implementation, authorization, scientific result, or registered-data access.

## Controlling result

A5XE does not close the evidence/encoding contract. Its two derivations agree, but they agree on a counterexample that violates the accepted V1→A5 contract and is nevertheless classified `VALID_SCIENTIFIC_RESULT / REPLICATED` with P1–P5 true.

The accepted science is sufficiently clear to repair these defects without a new scientific choice. Every blocker is therefore **Class B**.

## Blocking findings

1. The primary and null models use every TRAIN/TEST row, not the frozen original joint-support populations.
2. P4 score/margin controls and mandatory report-only diagnostics are asserted by names but not reconstructed or required semantically.
3. Sensitivity identity, factor path/values, configuration hashes, required outputs, support and provenance are not validated; producer-supplied coherence arrays determine the derived results.
4. `model_spec` is ignored and contradicts the hard-coded fitting loop (`12` recorded versus `100` used).
5. Machine validation leaves operative model, observed, validity, population and sensitivity fields semantically unchecked.
6. The transitive ledger omits A1 and therefore is not the complete claimed A1→A5 authority chain.
7. The raw derivation is hard-coded to a synthetic-only bundle and does not encode a future registered raw-evidence schema sufficient to reconstruct the accepted model/support/diagnostic objects.

Decision: reject as Class B. A5XE itself was not edited.
