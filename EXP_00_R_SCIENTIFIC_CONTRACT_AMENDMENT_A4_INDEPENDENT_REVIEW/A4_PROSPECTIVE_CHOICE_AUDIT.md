# A4 Prospective-Choice Audit

## N4 full-training LOO distance

**Verdict: local and defensible.** A3-R16 required a choice among self-inclusive, full-training LOO, and OOF distances. Full-training LOO is outcome-blind, matches support-threshold geometry, avoids degenerate self zeros, changes only the N4 cutpoint reference vector, and leaves recipient covariates/support/populations fixed. Prose and machine agree.

## Rössler classification precedence

**Verdict: local and defensible.** V1 partial/non-replication branches overlapped when a positive core failed required null evidence. A4 explicitly assigns such a state to partial replication, requires no resolved-negative carrier, and orders invalid → replicated → partial → not replicated. This is prospective, exhaustive, and does not alter the endpoint or estimand. Required-null failure becomes partial only under the explicit amended positive-core rule.

## Undeclared P4 choice

**Verdict: blocker.** Frozen V1 system proposition P4 says every preregistered carrier/leave-one-representation attribution analysis must retain positive direction and log-loss gain. V1 separately preregisters TRAJECTORY-only and LEARNED_FIELD-only outcome prediction. A4 declares those report-only and sets `leave_one_representation_coherence_registry=[]`.

It is reasonable that one remaining representation cannot yield pairwise coherence, but V1 does not uniquely imply that the associated attribution analyses have no P4 effect. Choosing report-only weakens a replication criterion and can favor a positive label. This choice was not included in `authority.new_prospective_scientific_choices`. The reviewer cannot choose the alternative on A4's behalf. A5 must prospectively state the exact P4 attribution estimands and pass/fail effect.

