# A4 N1–N5 Zero-Discretion Audit

## N1–N4

Independent reconstruction matches A4:

- fixed original jointly supported OOF/test rows, including zero carrier actions;
- original support frozen; no null-specific deletion/intersection/imputation;
- exactly 200 slots; any invalid slot invalidates with no retry/redraw/omission;
- canonical rows/actions/donors/strata and typed object-local PCG64 streams;
- N1 forward `a→π(a)`, complete OOF/full refits, direct physical-action outcome lookup;
- N2 within-seed FIELD-rank permutation without replacement and frozen observed carriers/outcomes;
- N3 different-seed donor with replacement, right-closed phase construction, geometric clockwise decreasing-index merge;
- N4 full-training LOO cutpoints, original recipient distance, deterministic higher-first merge, carrier-specific T/F subworlds;
- `k=count(null>=observed)`, adverse ties, `(1+k)/201`, `k<=4`.

Equal nearest-neighbor distances in N4 do not require donor tie-breaking: only the scalar minimum distance enters the cutpoint vector. Duplicate nonself rows legitimately contribute zero. Quantile equality/right insertion is explicit.

## N5

N5-SYNTH and N5-RUN are separated and correctly staged. Independent determinant/lexicographic generation reproduces the 12 matrices. Coordinates, every action-conditioned training path, queries, `B`, standardizer, target, support, and both fits transform; terminal states inverse-register before original scoring. Kendall tau-b undefined cases, minimum aggregation, `>=.99`, fixed populations, and failure mappings are complete.

**N1–N5 zero-discretion verdict: PASS.** This does not cure P4/validity encoding defects outside the null construction.

