# A4 Prose ↔ Machine Isomorphism Audit

## Matches

N4 choice, RNG/order, binning, N1–N5, Monte Carlo, primary P1–P3, amplitude P5, classification precedence, and Lorenz ceiling have matching prose/machine structures.

## Failures

| Operative prose/machine item | Mismatch |
|---|---|
| P4 attribution | V1 requires every preregistered carrier/leave-one attribution; A4 prose/machine reclassify single-representation analyses as report-only without a declared prospective choice. |
| mandatory sensitivity reports | prose requires coefficient, gain, support, provenance for each; machine has axes and a generic completeness gate but no output schema. |
| validity gates | prose describes substantive checks; machine stores mostly gate names. |
| seed-dominance identity | machine adds zero tolerance; prose does not type operands and accepted A2 does not justify that field. |
| authority/prose sealing | machine stores a V1 composite string; validator does not recompute it and does not hash A4 prose. |

The ledger's assertion that no operative prose-only/machine-only rule exists is therefore false. **Isomorphism verdict: FAIL.**

