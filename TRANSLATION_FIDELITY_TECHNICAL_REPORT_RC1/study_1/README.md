# Study 1 — Minimal translation study

This directory preserves the frozen exploratory precursor artifacts. Start with
`02_MINIMAL_TRANSLATION_EXPERIMENT.md`, then inspect `translation_spec.json`,
`translation_study_result.json`, and `TRANSLATION_STUDY_VERIFICATION.md`.

The runner is byte-identical to its source and expects an external historical
checkout at a hard-coded path. See `../REPRODUCIBILITY.md` and do not edit the
runner or primary result inside this release candidate.

