# Study 2 — Preregistered replication

This directory preserves the frozen replication protocol, runner, result,
manifest, freeze record, replay record, and source report. The primary result is
`replication_results.json`; its recorded replay is byte-identical.

The runner shares Study 1's external historical-checkout and hard-coded-path
limitation. See `../REPRODUCIBILITY.md`. Do not modify frozen artifacts.

