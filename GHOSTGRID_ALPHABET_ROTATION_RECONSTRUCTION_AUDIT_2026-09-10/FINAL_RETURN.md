# Final return

The arithmetic square, exact 1000x1000 checkerboard, entropy typing, modular class counts, exact coordinate transformations, raster rotations, and sampling controls are complete.

Exact coordinate permutations are lossless. Arbitrary-angle raster rotations and checkerboard downsampling are operator-dependent and lossy. The generated images are audit outputs tied to the included arrays and parameters, not independent evidence.

Legacy visual binding remains unresolved but does not block the numerical result. No physical or biological interpretation is supported.

Multi-resolution addendum: exact 11-cell nesting fails under cell-count interpretation but succeeds when 111 denotes sample points defining 110 intervals. The modulo-11 prime stick is exact and indexing-dependent. It is a software observation operator, not a demonstrated physical sensor.
