# Pass B — Minimum Existing Legend

This legend contains only distinctions already stated inside the frozen Lab 0.4
report and HTML instrument.

- **Source identities:** A, B, C and D are four distinct synthetic source
  threads.
- **Projection:** the displayed record is a declared orthographic projection of
  the source; views are 0°, 90°, 180° and 270°.
- **View behavior:** A and B remain distinct at 0° and 180° and coincide in the
  displayed projection at 90° and 270°; their source identities remain distinct.
- **Mask order:** the representation mask is applied after projection and does
  not delete the source.
- **Record classes:** `GENERATED_UNMASKED`,
  `GENERATED_MASKED_RECOVERABLE`, `UNKNOWN` and `BOUNDARY`.
- **Full trace:** masked samples are recoverable only when a complete generated
  trace has been declared; otherwise the masked interval is unknown.
- **Boundary:** projected coincidence does not establish physical identity or
  coupling. The test is synthetic and is not measured astronomy.

The legend says nothing about an Experience representation, ORION Intake,
scientific validation, OLS transfer or a universal translation grammar.
