# Proven operational object

On the frozen registered ORION typed domain, `H6`, `R5` and `E23` generate a
faithful operational action isomorphic to `(Z_2)^3=F_2^3` under the registered
transport and canonical serialization contract.

# Seven nonidentity transformations

`H7={H6,R5,E23,H6R5,H6E23,R5E23,H6R5E23}`.

# Known invariants and transport properties

Each generator is a total deterministic involution; all pairs commute; the
kernel is trivial; all eight transformations are distinct; closure and exact
source, phase, orientation, relation and `kappa` transport pass; SCALE is
fixed; `c_ref` remains external.

# Unresolved interpretation questions and prohibition

Generality beyond cyclic `Z12`, terminal-bearing domains, and the registered
representation remains unresolved. No retrospective visual confirmation is
permitted: historical octagon, heptagon, direction, frequency, prime,
Critical-Line, LEE or physical imagery is neither explained nor predicted by
this operational result.
