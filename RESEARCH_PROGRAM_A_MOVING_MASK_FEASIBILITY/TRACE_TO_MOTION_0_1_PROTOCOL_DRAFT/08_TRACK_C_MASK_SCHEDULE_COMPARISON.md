# Track C — Mask-Schedule Comparison

Status: `DRAFT — DESIGN INCOMPLETE`

V1's static-center selection from a completed trajectory is prohibited.
Schedules must be frozen before Human data and declare ID, class, coordinate
frame, geometry, width, center function, boundary rule, time parameter,
applicable cases, creation basis, hash and Owner reference. Human values remain
`UNASSIGNED`; synthetic values are never defaults.

For each source and schedule report hidden count/fraction, gap count and length
distribution, maximum gap, endpoint censoring, marker exposure, hidden
curvature where defined, interpolation proxies, observable support and common
support. Equal area and count are necessary descriptions, not sufficient
comparability.

Report separately:

1. availability and `UNKNOWN` behavior;
2. conditional error on each schedule's bounded support;
3. error on the common evaluable intersection.

Absent common support yields `BLOCKED_COMMON_SUPPORT`. Six paths remain
individual diagnostic cases. Without accepted matching or modelling, results
describe complete schedules only; causal attribution to motion is prohibited.
