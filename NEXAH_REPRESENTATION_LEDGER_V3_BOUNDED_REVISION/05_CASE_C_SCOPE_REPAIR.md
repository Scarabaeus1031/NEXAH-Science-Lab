# Case C Scope Repair

Case C remains a composite pipeline, not a simplified edge. The rejected v1 source description and corrected code-derived boundary both remain in claim history.

Corrections:

- the source boundary no longer lists the result file as an input artifact; raw fixture construction and frozen protocol are the source trace;
- approximate recovery applies only to subpath `C.C1 → C.C2`;
- primary-label information reduction applies to an explicit derived projection from the full record, not the composite edge;
- whole-edge classification-record construction remains separately supported;
- the prior source-boundary conflict remains `REJECTED` and is contradicted by the corrected claim through a stable relation.

