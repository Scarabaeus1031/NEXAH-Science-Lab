# Case D History Regression

Sample D is record revision 2 in a stable series. It carries the original rejected decoder-independent hierarchy claim as claim revision 1 and adds claim revision 2 that refines its scope to the bounded internal evidence. The relation is `REFINES`; it does not overwrite or reverse revision 1.

Execution remains `REPRODUCED`, reproducibility `REPRODUCIBLE`, and aggregate claim support `MIXED`. The rejected interpretation remains directly addressable by stable claim ID/revision.

A future revision in the same series must reference this record. It may supersede/refine/contradict/retract a claim, but the previous claim entry must remain addressable. A genuinely unrelated claim receives a new claim ID without a forced relation.

