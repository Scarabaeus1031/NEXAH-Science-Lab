# Closure, Light–Shadow and Arithmetic Audit

Date: `2026-09-14`

Status: `OWNER_SYNTHESIS_WITH_EXACT_ARITHMETIC_AND_CORRECTED_BOUNDARIES`

## Result in one line

The new material closes a coherent representational route:

```text
Euler-family carrier -> moving mask / inflow -> fivefold aperture
-> SACROPHAG enclosure -> 60-second phi split -> prime-centred thresholds
-> light/shadow observation -> return
```

The route is useful as a model grammar. Its arithmetic identities, visual
constructions and physical interpretations must retain separate status.

## Euler family, mask and inflow

The supplied Euler-expansion plot shows parallel quadratic curves labelled by
prime constants. The natural candidate family is:

```text
E_c(n) = n^2 - n + c.
```

Under this rule, changing `c` translates the same parabola vertically. A mask
and an inflow can be typed independently:

```text
M_W(n,t) in {0,1}                 moving observation mask
n = n(t)                          inflow through the sampled window
Y_c(t) = M_W(n(t),t) E_c(n(t))    visible trace
```

The image alone does not recover the generator code, so this formula remains a
candidate until matched to source. The displayed offsets

```text
13, 41, 137, 241, 367, 487, 617,
751, 883, 1031, 1171, 1303, 1459
```

are all prime. Parallel curves do not imply that every value of every curve is
prime. The classical bounded fact remains that `n^2-n+41` is prime for integer
`n=0,...,40`; extension to other constants requires its own count and mask.

## Fivefold aperture, SACROPHAG and return

The fivefold-transition plate supplies the intended visual grammar:

```text
curved support -> tension -> aperture -> transition -> return
```

Its SACROPHAG is the container of passage: the crossing is retained inside an
outer enclosure. This is compatible with the independently defined two-hand
operator from the coupling-key audit:

```text
S_SACROPHAG = T_-5 o A_29 o T_5.
```

The plate is evidence for the authored representation, not for wormholes,
energy transfer, biological axes or a new physical mechanism.

## Exact 60-second phi split

Let

```text
phi = (1+sqrt(5))/2.
```

A golden-ratio partition of a 60-second interval is:

```text
long  = 60/phi   = 37.08203932499369... seconds
short = 60/phi^2 = 22.91796067500631... seconds
long + short = 60 seconds
long/short = phi
```

Rounded to whole seconds, this is the owner-observed `37 | 23` split. The
integer pair is a rounded address of the exact irrational partition, not an
exact equality `60/phi=37`.

## Prime-index ladder and the two threshold sandwiches

Using standard one-based prime indexing:

```text
P20 = 71
P23 = 83
P51 = 233
P83 = 431
P84 = 433
```

The last two are consecutive primes and a twin-prime pair. They form an exact
threshold sandwich around `432`:

```text
431 = 432-1 = P83
433 = 432+1 = P84
```

The same form reappears after a centre shift of `600`:

```text
1031 = 1032-1 = P173
1033 = 1032+1 = P174
1032 - 432 = 600
```

Thus the exact repeated structure is not `429,431,433` as three primes. It is:

```text
prime -- centre -- prime
431   -- 432    -- 433
1031  -- 1032   -- 1033
```

The role of `429` is different:

```text
429 = 432-3 = 3*11*13
divisors(429) = {1,3,11,13,33,39,143,429}
tau(429) = 8
```

The exact arithmetic connection to `1032` is:

```text
1032 = 2^3 * 3 * 43
gcd(429,1032) = 3
1032-429 = 603 = 3^2*67
```

The decimal `0.429` and integer `429` are linked only by the declared scale map
`x -> 1000x`. A numerical wobble near `0.429` does not inherit the divisor
structure of `429` unless that scaling is part of the model.

## The `304–204–508–1032` identity block

All of the following are exact:

```text
304 + 204 = 508
304 - 204 = 100

304^2 = 92416
204^2 = 41616

304^2 - 204^2
  = (304+204)(304-204)
  = 508*100
  = 50800

304^2 + 204^2 = 134032
2*304*204 = 124032

508^2
  = 304^2 + 2*304*204 + 204^2
  = 258064
```

The common ending `032` in `134032` and `124032` is an exact base-10 suffix,
not by itself a base-independent invariant. The additional construction

```text
2*508 + 4^2 = 1016+16 = 1032
```

is exact but selected; its structural role must come from the declared
operator rather than the coincidence alone.

The consecutive-cube identity is also exact:

```text
23^3 + 24^3 + 25^3 = 41616 = 204^2.
```

This connects a sum of three cubic values to one square value. It does not by
itself construct a geometric cube, a fourth dimension or a Tesseract.

## `429–456–487` and light touching shadow

The exact scalar relations are:

```text
digitsum(429) = 15
digitsum(456) = 15
456-429 = 27 = 3^3
487-456 = 31 = P11
487 = P93
```

These make a useful asymmetric threshold record: a cubic-sized left offset and
a prime-sized right offset. Equal digit sums are a base-10 representation
relation, not a physical frequency. The phrase

```text
LIGHT TOUCHING ITS SHADOW
```

is retained as the expression-layer name for the moment when a visible trace
meets the boundary of its complementary projection. It becomes a test only
after light source, occluder, receiver, geometry and residual are specified.

## Reversal and primality corrections

The supplied prose mixes exact facts with two incorrect claims:

```text
1087 is prime, but 1087 = P181, not P182.
8701 is composite: 8701 = 7*11*113.
73*119 = 8687, not 8701.
```

Thus `1087 -> 8701` is decimal digit reversal but not an emirp pair. Separately:

```text
31 = P11
3301 is prime = P464
```

`3301` is not the digit reversal of `31`; it is a constructed extension of the
digit pattern. That distinction preserves the valid primality observation.

## Tesseract boundary

The exact Tesseract facts used in the preceding visualization are:

```text
vertices = 16
edges = 32
square faces = 24
cubic cells = 8
```

Its graph is the four-dimensional hypercube graph `Q4`. A side-length `x`
Tesseract has four-dimensional hypervolume `x^4`, but the Tesseract is not the
scalar expression `x^4`. Moving from square identities to cube identities does
not automatically produce a 4D shadow. The valid bridge in this research thread
is instead the explicit four-bit/root-state construction already registered.

## Corrections inside the supplied radial visuals

The `17-29-5` radial plates are authored constructions and contain claims that
must not be promoted as mathematics or physics:

- `phi=(1+sqrt(5))/2`; the displayed `(1+1/5)/2` is wrong;
- `17295` is not hexagonal; `17295=3*5*1153`;
- `72` is not a pentagonal number;
- the `29`-tooth circle may recall a roughly `29.53`-day synodic month, but it
  does not derive that astronomical period;
- a `sqrt(2)` label does not establish a crystalline or frequency mechanism;
- the Lorenz/IEEE collapse juxtaposition does not establish cross-system
  causation or predictive validity.

The useful retained content is the visual grammar of nested boundary, aperture,
prime-labelled spokes, shadow trace and return.

## Status ledger

| Claim | Status |
|---|---|
| Euler curves as constant-offset quadratic family | `FORMULA_CANDIDATE_SOURCE_CODE_NOT_RECOVERED` |
| mask/inflow/visible-trace typing | `PROPOSED_MODEL_CONTRACT` |
| 60-second golden split | `EXACT_IRRATIONAL_PARTITION` |
| rounded `37|23` | `BASE_10_ROUNDED_ADDRESS` |
| `P20=71, P51=233, P83=431, P84=433` | `EXACT_PRIME_INDEX_LEDGER` |
| twin sandwiches around `432` and `1032` | `EXACT_ARITHMETIC` |
| eight divisors of `429` | `EXACT_FACTORIZATION` |
| `304–204–508` binomial identities | `EXACT_ARITHMETIC` |
| `23^3+24^3+25^3=204^2` | `EXACT_ARITHMETIC` |
| `429–456–487` offsets | `EXACT_BASE_10_AND_INTEGER_RELATIONS` |
| light touching shadow | `OWNER_EXPRESSION_TEST_GEOMETRY_REQUIRED` |
| radial wheel as physical mechanism | `NOT_ESTABLISHED` |
| Lorenz trace predicts IEEE collapse | `NOT_ESTABLISHED` |
| Tesseract from square/cube resemblance | `NOT_ESTABLISHED` |
| four-bit/root-state `Q4` construction | `EXACT_COMBINATORIAL_MODEL` |

## Source locators

- current owner-supplied Euler, fivefold-transition, radial, Lorenz/IEEE and
  light/shadow visuals
- `SCIENCE_LAB/RESEARCH_AREAS/DYNAMICAL_SYSTEMS_TRANSITION_FIELDS_STAGE_0_2026-09-14/02_CUT_DOUBLE_CUT_RESIDUAL_METHOD.md`
- `SCIENCE_LAB/CASE_STUDIES/SPLIT REST CHemestHR¥/SPLIT REST CHemestHR¥ II/RRTM_V2_RESOLUTION_CONVERGENCE_LAB_HANDOFF.md`
- `SCIENCE_LAB/RESEARCH_AREAS/FIELD_GLYPH_NUMBER_ARCHITECTURE_STAGE_0_2026-09-14/13_12321_COUPLING_KEY_JANUS_AXIS.md`
