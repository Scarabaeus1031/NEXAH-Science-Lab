# Final FGP-01 Decision

The frozen architecture materially distinguishes structure, registered instance, state/time, projection, represented figure, criterion-relative organization, interpretation and source identity. Structure/instance/state and time/trajectory use existing identity, relation, state, event and history types. Projection uses existing ObservationMap/OperationRule/View boundaries. Figure is a source-typed View composition.

Gestalt is not already a primitive, but no primitive or RID schema extension is required. A useful derived record can cite the figure, observer/model/criterion, grouping relations, alternatives, uncertainty and provenance. The unchanged six-mark control supports two formal organizations under different criteria; this establishes representational non-collapse, not a perception theory.

Q° is completely representable as a projection-relative orientation marker. The mirror chain preserves object, signal/path, readout, figure, organization and interpretation as separate stages. Historical, numeric and philosophical materials provide no formal evidence.

Decision class: `B_DERIVED_GESTALT_OR_PROJECTION_RECORD_USEFUL_BUT_NO_NEW_PRIMITIVE`.

```text
FGP01_STATUS=CLOSED
NEXT_ACTION=STOP
```
