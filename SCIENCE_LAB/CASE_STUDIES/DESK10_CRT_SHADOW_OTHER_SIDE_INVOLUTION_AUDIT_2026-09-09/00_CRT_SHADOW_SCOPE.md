# CRT Shadow / Other-Side Involution Audit — Scope

Date: 2026-09-09  
Mode: bounded mathematical audit; additive record only.

## Locked object

- modulus: `N = 31000`
- selected pair: `x = 11357`, `S_N(x) = 19643`
- operator: `S_N(x)=(-x) mod N` on canonical representatives `0 <= x < N`
- CRT registers tested: `(31,1000)` and `(8,125,31)`
- decimal string operator tested separately: Backspace `B`

## Evidence boundary

The selected pair and `11357 + 19643 = 31000` are explicitly present in both Clockwork Orange manifests. The v3 manifest also types `19643` as `HIDDEN_SHADOW_NODE_19643` and says that it retains only the verified complement relation. Existing Science-Lab records separately bind `11357`, its factorization `41*277`, CRT reconstruction practice, a `GRID_ADDRESS(n)=(q,r)` definition for modulus 20, and a synthetic persistent-address method.

The exact operator `S_31000`, the CRT system `(31,1000)`, the equivalent system `(8,125,31)`, and the component tuples `(11,357)` and `(20,643)` were not located before this audit. They are new bounded formalizations derived from standard mathematics and applied to an already documented pair.

No source file containing the visual title `THE 29/31 GATE · CRT & SHADOW` was located by exact text search or targeted OCR. Its wording is therefore reviewed from the supplied prompt only; no image is modified.

## Non-actions

No source, closed workstream, ORION Core record, visual, EMP record, or historical audit was modified or reopened. No physical experiment, new physics claim, commit, push, publication, or capability activation was performed.
