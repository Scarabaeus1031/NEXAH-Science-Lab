# NTO Orientation Control

The candidate orientation relation is already expressible:

```text
declared reference
+ observation
+ typed relation
+ orientation rule
-> orientation result with provenance and uncertainty
```

This is a composition of existing records, not a new capability. The reference does not cause the observed system, a compass does not report truth, and orientation is not ontology.

`NTO_NEW_CAPABILITY_FOUND=NO`

Rejected: Saturn as a semantic sender; NTO as a receiver of cosmic messages; compass as truth device; spelling or glyph relations as operators.

