# Packets D, E, and F

Packet D is the listed window `{29,31,32,33,34,35}`. It contains two primes followed by four composites, but it is not a consecutive integer interval because 30 is absent.

Packet E `{29,32,34}` contains one prime and two even composites. Packet F `{31,33,35}` contains one prime and two odd semiprimes.

All classifications are exact for their members. The grouping itself is not shown to be intrinsic. No independent generative or selection rule was supplied that would establish E and F as arithmetic families rather than selected subpackets of D.

Therefore:

- member arithmetic: `INTRINSIC_TO_INTEGER`;
- D/E/F membership: `PACKET_DEPENDENT`;
- alleged family structure: `POST_HOC_SELECTION_OR_UNDERDEFINED`;
- shift invariance: `NOT_ESTABLISHED`.

