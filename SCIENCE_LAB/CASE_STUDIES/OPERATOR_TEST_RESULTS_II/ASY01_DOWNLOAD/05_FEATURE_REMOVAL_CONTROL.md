# 05 — Feature-Removal Control

Removing B from S1 exactly recovers S0. Consequently the symmetry group expands
from trivial `C1` back to continuous `SO(2)`, and registered orientation becomes
unidentifiable again.

```text
FEATURE_REMOVAL_RESTORES_SYMMETRY=YES
FEATURE_REMOVAL_REMOVES_ORIENTATION_INFORMATION=YES
```
