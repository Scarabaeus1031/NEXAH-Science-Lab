# 01 — CROA-01 Test Specification

## Question

Which predeclared operator, if any, recurs across at least two independently
specified representations with the same arity, compatible types, preserved
property and loss/introduction profile?

## Frozen evidence boundary

GPT-01 remains closed. Its only imported result is the representation-specific
180° mapping `G6 → G9` in the frozen DejaVu Sans Mono test. Glyph geometry is
not arithmetic and static form supplies no direction.

The audited representations are exactly R1–R6. R7 is quarantined provenance.
No new number family, font, glyph variant, geography, etymology, physical model
or symbolic meaning is introduced.

## Match rule

A cross-representation recurrence requires all four:

1. same operator type;
2. same input/output arity;
3. same preserved property;
4. same loss/introduction profile.

Local existence in one representation is insufficient. Neutral relabelling
must preserve the claimed operator.
