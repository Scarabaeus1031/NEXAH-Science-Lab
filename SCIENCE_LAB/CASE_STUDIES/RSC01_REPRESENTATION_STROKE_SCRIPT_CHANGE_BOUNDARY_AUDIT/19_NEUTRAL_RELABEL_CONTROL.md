# 19 — Neutral Relabel Control

All cultural and project labels are removed:

```text
A = production tool
B = executed movement
C = visible trace
D = recognized form
E = system unit
F = contextual value
G = representation system
H = relational participant
I = relation
J = evidence record
```

## Production dependency

```text
A + medium + substrate + parameters -> B -> C
C -> partial evidence about B in J
C -> D -> E
E + G + context -> F
```

## Script dependency

```text
G1 and G2 can represent language L using non-identical D/E/F mappings
G1 != G2
G change != L identity change
G change != no representational change
J is required for each asserted correspondence
```

## Relational-reference dependency

```text
E + H(speaker) + H(addressee/referent) + I + discourse context -> F
H identity != I classification != selected E
```

The distinctions survive without China, Vietnam, Asia, script names, kin terms or NEXAH.

`NEUTRAL_RELABEL_SURVIVES=YES`

