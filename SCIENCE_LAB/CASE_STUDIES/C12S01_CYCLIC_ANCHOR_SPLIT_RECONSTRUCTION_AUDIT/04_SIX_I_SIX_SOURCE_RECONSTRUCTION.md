# 6 + I + 6 Source Reconstruction

## Materially attested content

The recovered corpus contains the heading `The 6 + I + 6 Geometry — The Escape Mechanism`. It describes a double-pyramid form oscillating between positive and negative domains and calls `I` the central axis and an imaginary bridge. A second document repeats `6+I+6 Escape Geometry (C6–HII–C6 chain)`.

This establishes that the expression and a stated axial interpretation occur in the historical corpus.

## Formal typing

| token | defensible type | rejected collapse |
|---|---|---|
| first `6` | source-side cardinal or label, exact inventory unspecified | not automatically `C6`, six nodes, a hexagon, or residue 6 |
| `+` | source separator/composition mark, operation unspecified | not established as arithmetic addition |
| `I` | central-axis label with an imaginary interpretation | not integer `1`; not automatically the complex unit in a defined algebra |
| second `6` | second source-side cardinal or label, exact inventory unspecified | not automatically a mirror copy or another `C6` action |

## Missing formal evidence

The source does not provide:

- a set of thirteen objects;
- six named upper and six named lower elements;
- a central counted node;
- graph vertices or edges;
- coordinates, incidence, metric, symmetry group, or transformation;
- an equation that defines `+`;
- a mapping to `Z/12Z` or the clock anchor.

## Decision

`SIX_I_SIX_HISTORICALLY_ATTESTED = YES`

`SIX_I_SIX_FORMALLY_DEFINED = NO`

`I_DISTINCT_FROM_ONE_PRESERVED = YES`

The historical discovery is real but does not repair the missing formal geometry.
