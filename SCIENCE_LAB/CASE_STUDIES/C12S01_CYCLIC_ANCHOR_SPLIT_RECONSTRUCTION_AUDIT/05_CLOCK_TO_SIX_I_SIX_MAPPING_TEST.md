# Clock-to-6+I+6 Mapping Test

## Candidate comparison

| property | cyclic clock model | historical `6+I+6` |
|---|---|---|
| formal carrier | `Z/12Z` | not specified |
| anchor | chosen residue `[0]` | `I` described as an axis |
| local structure | predecessor/anchor/successor | two `6` labels separated by `I` |
| operation | modular addition | `+` not typed |
| count | 12 residues | possible visual/cardinal language, inventory absent |
| attestation | clock motif plus separate 11–12–13 sequence | expression and double-pyramid wording attested |

## Tested candidate mappings

1. `I → 1`: rejected because the source calls `I` an axis/imaginary bridge, not integer one.
2. `I → [0]`: unsupported; no source rule identifies the axis with the clock anchor.
3. two sixes → two semicircles: compatible imagery only; no partition rule is supplied.
4. `6+I+6 → 13 counted objects`: unsupported because `I` is not established as a counted object and `+` is not arithmetic.
5. `6+I+6 → residues around 12`: structurally mismatched; the clock neighborhood has three typed residues, not two six-element domains.

## Result

`CLOCK_TO_SIX_I_SIX_MAPPING_STATUS = NO_MAPPING`

The two objects can coexist in the archive. No function, homomorphism, quotient, embedding, or reconstruction rule between them is evidenced.
