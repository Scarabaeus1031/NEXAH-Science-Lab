# Positive Controls

## 1. Geometry branch

One `K_(1,3)` graph retains adjacency and degree while `E1` and `E2` produce different visible angles. Passed.

## 2. Measurement branch

One vehicle state supports distinct speed and rpm observables; different transmission states break mutual identification between their values. Passed.

## 3. Reduction branch

Different carrier/selection structures can reduce to the same ratio, so the displayed ratio cannot reconstruct its source. Passed.

## 4. Mnemonic branch

The month-length fact survives replacement of the hand mnemonic by a table or text, and July/August show that physical alternation is not the calendar rule. Passed.

## 5. Composite view

A dashboard or annotated geometry can combine an embedding and measurements while retaining two provenance branches. Passed.

`POSITIVE_CONTROLS=5_OF_5_PASSED`
