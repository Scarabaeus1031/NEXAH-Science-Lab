# Splinter / V / Pyramid Control

Visible morphology was screened before measurement:

- IMG_02 contains V-, wedge-, axial-, and pyramidal-looking decorative forms;
- IMG_03/04/05 contain one straight triangular motif admitted as an image-plane triangle;
- IMG_01/06 contain curved creature/ornamental morphology;
- IMG_07 contains diagram connectors and polygonal nodes.

The label `SPLINTER` is not visibly attested. No splinter operation, cut, vector, axis, or operator is recoverable.

The admitted triangle's pixel-line orientations produce three internal angles summing to 180° by construction of the line intersections. This is a standard Euclidean image-plane control, not evidence of a pyramid or dimension.

- `V != TRIANGLE`
- `TRIANGLE != PYRAMID`
- `PYRAMID != DIMENSION`
- `SPLINTER != AXIS`
- `SPLINTER != CUT`
- `SPLINTER != OPERATOR`

