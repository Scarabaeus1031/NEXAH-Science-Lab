# Axis → Cut → View Control

The images contain axes, triangular boundaries, folds, fields, paired panels, and different render layouts. They do not define an operational chain `AXIS -> CUT -> VIEW`.

For `OBJ_A`:

- `X`: a repeated image-plane triangular motif;
- possible operation `X->X'`: not registered;
- changed quantity: rendered line orientation/aspect;
- preserved quantity: recognizable layout correspondence only;
- transformation without historical labels: unavailable;
- reproducibility: pixel measurements yes, source transformation no.

No explicit intersection/section/clipping boundary or camera/frame mapping connects the three files.

`AXIS_CUT_VIEW_TRANSFORMATION_RECOVERED=NO`

