# GIJ-01 - GTI / GTA / GIT Controls

All three strings begin with zero evidential meaning. Familiar product, entertainment, software or institutional associations are quarantined.

| Control | Neutral operation | Result |
|---|---|---|
| GTI | Replace the event label `J` with neutral `T`. | The dependency model is unchanged. `J` is not privileged. |
| GTA | Replace instruction/event labels with arbitrary `T/A`. | Suggestive labels cannot create a relation; typed definitions remain necessary. |
| GIT | Use `G/I/T` as arbitrary symbols only. | The string does not independently imply version control or an execution model. |
| RANDOM RELABEL | Replace the formal roles with unrelated letters. | The structure survives. |

```text
GTI_CONTROL_SURVIVES=YES
GTA_CONTROL_SURVIVES=YES
GIT_CONTROL_SURVIVES=YES
RANDOM_RELABEL_SURVIVES=YES
```

## Optional real-world control

The real-world Git and GitHub control was skipped. GIJ-01 does not depend on it, and no external documentation was required.

```text
GIT_EQUALS_GITHUB=NO
REAL_WORLD_GIT_CONTROL_USED=NO
GITHUB_CONTROL_USED=NO
```

The `GIT != GITHUB` statement is retained only as the owner-supplied scope boundary; no Git or GitHub implementation claim is made.

