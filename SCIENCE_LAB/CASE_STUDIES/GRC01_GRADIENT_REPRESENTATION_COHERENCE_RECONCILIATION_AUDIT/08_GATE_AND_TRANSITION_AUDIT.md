# 08 — Gate and Transition Audit

The following remain distinct: low density, low coherence, high gradient, trajectory crossing, separatrix/boundary, transition region, and gate candidate.

## Operational-definition check

V2 displays vertical lines and a coherence curve; V4 displays “gate candidates” in panels described as low density + low coherence; V7–V9 use aperture/event terminology. These show conventions in rendered outputs (`D`). They do not provide one recovered criterion specifying variables, Boolean logic, thresholds, windowing, crossing rule, persistence, deduplication, and edge cases.

The existing archaeology audit additionally records that a historical Gate quantity did not reliably detect transitions. That is a bounded negative prior result, not evidence for a new gate.

Without a source-bound criterion and matched arrays/run record, translation, rotation, rescaling, sampling, KDE bandwidth, grid resolution, and threshold survival cannot be empirically checked. Analytically, coordinate-covariant constructions can survive translation/rotation if the metric and fields transform consistently; numerical threshold sets generally remain parameter dependent.

`LOW_COHERENCE_EQUALS_GATE=NO`  
`GATE_OPERATIONAL_DEFINITION_STATUS=UNDERDEFINED`  
`GATE_REPRODUCIBILITY_STATUS=INCOMPLETE`  
`GRADIENT_PREDICTS_TRANSITION=NO`
