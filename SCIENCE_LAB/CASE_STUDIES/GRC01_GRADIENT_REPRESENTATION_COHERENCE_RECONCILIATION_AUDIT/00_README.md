# GRC-01 — Gradient / Representation / Coherence Reconciliation Audit

Status: `CLOSED`  
Mode: `BOUNDED_FORMAL_AND_REPOSITORY_RECONCILIATION_AUDIT`

This package reconciles the proposed historical chain

`trajectory samples -> estimated density -> density gradient -> flow comparison -> local relation metric -> thresholded candidate region`

without treating the chain as validation. The result is mixed: standard mathematical stages are clear; one separate archived synthetic-field lineage contains an implemented finite-difference gradient; the supplied trajectory/density/coherence visuals are not joined to recoverable generator code, parameters, arrays, or run records in the inspected repository.

Primary result: `F_MIXED_RESULT_REQUIRES_COMPONENTWISE_CLASSIFICATION`.

No experiment was run. No predecessor, architecture, operator, ontology, implementation, or scientific claim was changed. The nine supplied images were inspected read-only and were not copied or modified.

## Classification key

- `A`: standard mathematical definition
- `B`: implemented/computed repository quantity
- `C`: empirical observation from a recorded run
- `D`: visualization / representation
- `E`: interpretation
- `F`: unsupported or underdefined historical claim

## Files

The numbered files proceed from provenance and frozen boundaries through the component audits, controls, nonclaims, and final decision. `GRC01_RESULTS.json` is the machine-readable closeout.

