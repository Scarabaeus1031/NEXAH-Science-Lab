# Grid-to-3D Control

## Historical 3×3×3 candidate

No bounded source object was recovered that supplies all of:

- 27 stable IDs;
- three declared indices and their meanings;
- coordinates for `ψC`, `θFlow`, and `φK`;
- a layer-to-cube transformation;
- a tensor carrier, rank/order, components, and transformation law.

The recovered image `The_Prime_Resonance_Cube_11x12x13.png` is an identical duplicate in two locations, but its title and cube-like appearance do not define a 3×3×3 tensor or connect it to T/G/Q layers.

## Classification

`HISTORICAL_3X3X3_STATUS=UNDERDEFINED`

`TENSOR_STATUS=LABEL_ONLY_OR_UNDERDEFINED`

`27_POSITIONS_RECONSTRUCTABLE=NO`

## Conditional mathematical control

If a future source declared IDs on `{0,1,2}³`, orthographic projection `(i,j,k)→(i,j)` would preserve `i,j`, lose `k` unless encoded, and may merge up to three objects at one displayed position. This generic fact is not adopted as the historical transformation.

Data index, representation coordinate, and visual position remain distinct.
