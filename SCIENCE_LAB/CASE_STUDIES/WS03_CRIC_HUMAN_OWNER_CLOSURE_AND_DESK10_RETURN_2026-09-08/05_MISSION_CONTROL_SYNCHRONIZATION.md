# Mission Control Synchronization

The authoritative Mission Control surfaces were identified as:

- `DECISION_LOG.md` for Human semantic decisions;
- `REGISTRIES.md` for pipeline and bounded handoff state;
- `MISSION_CONTROL.md` for current operating orientation;
- `OVERVIEW.md` for THE EYE portfolio-level currentness.

The next available semantic decision ID is `D-032`. The bounded handoff ID is `WS03_CRIC_CLOSURE_RETURN`.

The synchronization is limited to WS03 closure. It creates no ticket, WIP, mission activation, portfolio reopening, public projection or lifecycle change for another workstream.

Exact before/after hashes and the separate Mission Control sync-package manifest are recorded during final validation.
