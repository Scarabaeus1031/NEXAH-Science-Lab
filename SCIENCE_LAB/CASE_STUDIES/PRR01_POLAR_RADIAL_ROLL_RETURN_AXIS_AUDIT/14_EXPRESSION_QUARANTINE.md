# 14 — Expression Quarantine

The following Human associations are preserved as expression only:

- `KK`, `K`;
- C-/E-/S-/M-Class;
- `S4`, `S2`;
- `FORW | ARD`;
- `ROLL | RAD`;
- vehicle brand/class sequences;
- AUTO/MANUAL word associations.

```text
FORW_ARD_STATUS=EXPRESSION_ONLY
ROLL_RAD_STATUS=EXPRESSION_ONLY
VEHICLE_CLASS_LETTER_STATUS=EXPRESSION_ONLY
EVIDENTIAL_WEIGHT=ZERO
```

No inference is made that K means coupling/Cardan, class letters form a hierarchy, S4/S2 encode wheel counts, word segmentation creates mechanics, Kardan means quaternion `k`, or AUTO means autonomous logic.

