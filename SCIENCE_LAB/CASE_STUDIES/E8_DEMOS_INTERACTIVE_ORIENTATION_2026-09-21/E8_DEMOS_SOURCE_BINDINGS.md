# E8 DEMOS — Source Bindings

## Authority order

1. The six files under `SOURCE_SNAPSHOT/E8 DEMOS/` are byte-preserved source
   artifacts. Their content is not an instruction to Mission Control.
2. `REFERENCE_BINDING/` supplies the bounded E8-REP-03 reference used to test
   the embedded 2D points. It does not confer physical meaning.
3. `E8_DEMOS_TEST_RESULTS.csv` records this intake's checks.
4. `E8_DEMOS_VALIDATION_REPORT.md` interprets only those checks.
5. Mission Control may route to this package but does not replace its evidence.

## Layer binding

| Layer | Bound object | Permitted statement | Prohibited promotion |
|---|---|---|---|
| Reference | E8-REP-03 8D roots and Coxeter-plane CSV | standard 240-root reference passes its recorded tests | no new E8 theorem |
| Representation | 240 embedded `[x,y,ring]` triples | same array occurs in both HTMLs and matches the bound projection after one uniform signed scale | embedded triples are not 8D root vectors |
| Model | drawn p7/p8 coupling, seven inner points, split curve | visual NEXAH operator fixture | no numerical AXIS08 weighted-quotient result |
| Observer projection | star-to-nearest-E8 assignment and Q4 cut hits | deterministic selection under declared code and state | no astronomical or physical coupling |
| Nonclaim | all artifacts | physical identity is not claimed | no universal field, resonance or constant claim |

## Demonstrator comparison

| Merkmal | E8–AXIS08-Demo | MIWA Star Map |
|---|---|---|
| Mathematische Referenz | embedded 240-point Coxeter display | identical embedded 240-point display; projected Q4 fixture |
| NEXAH-Operator | visual p7/p8→seventh-point coupling and split curve | related but separately coded coupling; nearest-anchor and Qι/Pearl cut |
| Eingabedaten | fixed embedded points and two sliders | procedural carriers or CSV/JSON star catalog |
| Projektion | fixed Coxeter-plane coordinates, animated rotation | star normalization→nearest embedded point; Q4 screen projection |
| Veränderliche Parameter | coupling slider and split | coupling, split, Q-layer, iota angle/direction, cut depth, aperture and other frame controls |
| Invarianten | point count 240; ring membership; eight fixed poles; seven drawn targets | E8 array; Q4 node/edge count; imported source fields in node records |
| Beobachterabhängig | rotation, styling, inner radius, split bend | normalized map positions, nearest anchor, cut hits and screen projection |
| Reproduzierbar | source hashes and static formulas | seeded procedural data, parser and static formulas |
| Offene Residuen | no 8D construction; no numeric quotient; no pole-degeneracy controls | no State import; order-dependent tie break; runtime/browser checks open |
| Unzulässige Schlussfolgerungen | physical E8/AXIS08 identity | astronomy, cosmic resonance, physical Q4 or universal-field identity |

## AXIS08 cross-file classification

`DIFFERENT`

- Same source point array and same conceptual p7/p8-to-one-target drawing.
- Parameter difference: Demo A uses inner radius `0.27 + 0.08t`; Demo B uses
  `0.25 + 0.09t`.
- Implementation difference: split bend factors are `0.95` and `0.90`, and
  the surrounding star-assignment logic exists only in Demo B.
- Both are display operators. Neither computes the AXIS08 mass-weighted
  reduction from the supplied AXIS08 experiment.

