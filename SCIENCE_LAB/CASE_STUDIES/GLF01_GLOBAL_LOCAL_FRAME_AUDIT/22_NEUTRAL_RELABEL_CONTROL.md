# 22 — Neutral Relabel Control

Replace all project, calendar and cultural names with:

```text
X = global object
p = point
o = chosen origin
c = cut/seam
r = representation
f = local frame
P = partition
l = label
q = tested property
```

The core dependencies remain:

```text
r(X;o,c,l) != X
changing o,c,l may change coordinates without changing X
removing p from X may change X
f(p) gives local data and does not equal X
q is structural only if its stated transformation class is stable
P partitions a representation and does not create external meaning
```

Circle quotient, tangent linearization, polygon symmetry/parity and calendar cut dependence all survive removal of NEXAH, glyphs, month names and cultural labels.

`NEUTRAL_RELABEL_SURVIVES=YES`

