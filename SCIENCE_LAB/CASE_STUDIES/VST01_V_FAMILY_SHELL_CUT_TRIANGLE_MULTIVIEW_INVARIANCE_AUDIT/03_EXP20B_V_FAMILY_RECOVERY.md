# EXP20-B V-Family Recovery

EXP20-B was not physically available. No image, generator, input data, parameter record, transformation, or output-coordinate table could be inspected.

Accordingly:

- no two branches can be registered;
- no common origin or anchor can be established;
- no `theta_1`, `theta_2`, or minimal angular separation can be derived;
- no source relation can identify a pair;
- visual proximity cannot be assessed or promoted.

`V_FAMILY_RECOVERED=NO`

`V_PAIR_COUNT=0_VERIFIED`

This is an evidence limitation, not a claim that the historical image contains no V-like forms.

