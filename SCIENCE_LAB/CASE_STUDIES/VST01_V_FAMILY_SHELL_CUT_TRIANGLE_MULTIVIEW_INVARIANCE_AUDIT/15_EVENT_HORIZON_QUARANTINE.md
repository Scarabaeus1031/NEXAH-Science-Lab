# Event-Horizon Quarantine

The geometric control uses only the circle `r=R`, named `CONSTANT_RADIUS_SHELL`.

No source experiment was available to define a physical, relativistic, cosmological, or causal horizon. A circle in a polar plot is not a physical horizon.

`EVENT_HORIZON_STATUS=EXPRESSION_ONLY`

No event-horizon physics was used.

