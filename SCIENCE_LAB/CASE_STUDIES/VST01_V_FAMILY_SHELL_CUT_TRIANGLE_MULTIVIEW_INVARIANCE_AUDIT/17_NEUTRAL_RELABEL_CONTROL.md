# Neutral Relabel Control

The terms NEXAH, X, O, Y, II, Q, Q°, mirror, bridge, event horizon, RAD, Iota, resonance, persistent, and transition were removed.

The conditional model becomes:

- `NODE_A`: common point, if registered;
- `EDGE_1`, `EDGE_2`: two rays, if source-defined;
- `SHELL_R`: circle of radius `R` about `NODE_A`;
- `NODE_B`, `NODE_C`: shell intersections;
- `VIEW_1`, `VIEW_2`: alternative coordinate representations.

The standard implications remain: equal radii, conditional isosceles triangle, and chord formula. No source-defined EXP relation appears merely through relabeling.

`NEUTRAL_RELABEL_SURVIVES=YES`

