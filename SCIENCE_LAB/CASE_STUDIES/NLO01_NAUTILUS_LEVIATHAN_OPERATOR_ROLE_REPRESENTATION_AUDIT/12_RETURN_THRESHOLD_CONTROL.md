# Return / Threshold Control

The images visibly contain:

- VIS-01: `DEMIZEL -> Schwelle`, `STILL POINT -> Entkopplung`, Q° observer/measure language;
- VIS-02: Q° Zero-Node and the prose claim “Return is the only stable operator”;
- VIS-03/04/05: Grenzschwelle, mirror-axis, and inner-return-loop language.

These are historical expression assignments. None supplies a scale, typed map, execution, result, or formal identity.

PRR-01's `J_R(d)=R^2/d` remains the only recovered explicit return map in scope and is not generalized by these boards.

Rejected:

- `ZERO = STILL_POINT`;
- `ZERO = THRESHOLD`;
- `THRESHOLD = RETURN`;
- `RETURN = STABILITY`;
- `Q° = ZERO`;
- `Q° = MEASUREMENT`;
- `DEMIZEL = +0.5` as an intrinsic mathematical fact.

Numeric annotations remain expression annotations without a declared scale and mapping.

