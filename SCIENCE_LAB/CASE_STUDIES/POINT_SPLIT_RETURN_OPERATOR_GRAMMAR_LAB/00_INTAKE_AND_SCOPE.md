# Point–Split–Return Operator Grammar Lab — Intake and Scope

```yaml
case: POINT_SPLIT_RETURN_OPERATOR_GRAMMAR_LAB
desk: 10 Incoming Intake Steward
date: 2026-08-23
mode: BOUNDED_SYNTHETIC_GEOMETRY_OPERATOR_TEST
historical_images_as_ground_truth: NO
source_mutation: NO
commit: NO
```

## Question

Can a declared point–split–return object be distinguished from matched random graphs and from random fields searched after the fact, while remaining stable under declared transformations and raster resolutions?

The object is tested only as known Euclidean/projective geometry. “Needle cushion”, “Elevator”, “Five/Seven Release”, “Shadow”, “Crown”, “Q-Split”, and “Return” are not inherited as operators.

## Controlling prior findings

- RRTM V2-B: `A_FINITE_TRANSIENT_COLLAPSES`.
- Spatial Crown: `C_STRUCTURED_GEOMETRY_REQUIRED` within its preregistered synthetic family; historical origin unidentified.
- Return 17: `C_POST_HOC_CONSTRUCTION`.

Their files are inputs by hash and remain unchanged.

## Incoming check

`/Users/tho2020/Desktop/00_INCOMING` contains only `.DS_Store` and two already controlled packages: `Cikada 3301 Mandelbrot` and `CIKADA 3301 LATER MARKER CORPUS`. The latter is already classified `HOLD_CONFLICTING_DEFINITIONS`. No new unclassified package was admitted, moved, renamed, deduplicated, corrected, or executed.

## Phase-4 gate

No historical application is allowed unless synthetic gates G1–G7 all pass. In particular, an adversarial post-hoc false-positive rate above 5% stops the case before any historical scoring. Historical labels, filenames, colours, and number choices are excluded from metric design.

## Claim boundary

`KNOWN_EUCLIDEAN_AND_PROJECTIVE_GEOMETRY`; `SYNTHETIC_POINT_SPLIT_RETURN_OPERATOR_FAMILY`; no numerological causality, physical/astronomical inference, Cicada solution, universal symbol, historical-origin attribution, ORION activation, or cross-domain adoption.

