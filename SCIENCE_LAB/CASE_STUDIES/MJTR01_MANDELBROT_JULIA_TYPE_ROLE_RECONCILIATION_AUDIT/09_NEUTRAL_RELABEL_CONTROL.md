# 09 — Neutral Relabel Control

Replace all suggestive letters:

```text
A       parameter domain
a∈A     selected parameter value
F_a     parameterized map
X_a     exact generated object
E_a     finite operational representation
R       registered observation map
r=R(E_a)
K_r     compatible-source set
```

All membership, indexing, generation, numerical-representation, observation, and reconstruction distinctions survive. The execution boundary also survives:

`rule specification + selected a + selected state + actual application event → resulting state → accumulated sequence/trace`.

Nothing depends on `C`, `c`, `J`, `Q`, `i`, or `j`. Any letter-based association is expression-level only.

`NEUTRAL_RELABEL_SURVIVES=YES`

