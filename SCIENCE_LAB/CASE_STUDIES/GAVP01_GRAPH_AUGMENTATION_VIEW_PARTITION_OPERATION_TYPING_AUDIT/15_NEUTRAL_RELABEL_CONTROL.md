# Neutral Relabel Control

Remove NEXAH, `G+`, `G°`, `D`, View, ROOT, M, SM, REST, MASK and AUGMENT. Replace them with:

```text
Object_0
Structure_1
Map_A
Event_A1
Representation_B
DerivedObject_C
Observation_D
History_H
```

The conclusions survive:

- one map changes coordinates while preserving source adjacency;
- one registered construction adds a vertex, subdivides an edge and adds incidence;
- an induced angle decomposition is distinct from the construction;
- an ordered event history is distinct from its final embedding;
- one source admits multiple representations;
- observation, measurement, readout and representation remain separate;
- operation order is conditional rather than universally commutative;
- set difference, intersection, complement, occlusion and residual remain distinct.

`NEUTRAL_RELABEL_SURVIVES=YES`

No conclusion depends on candidate symbols or wording.
