# G+ / G° / D / View Notation Audit

| Candidate | Finding | Status |
|---|---|---|
| `G+` | useful as a local alias only when explicitly bound to a particular augmented graph and lineage; `+` does not mean positivity or arithmetic | `USEFUL_LOCAL_ALIAS_WITH_DEFINITION` |
| `G°` | intended uses reduce to `(G,E)`, transform provenance or `EmbeddingRef`; the degree mark adds ambiguity rather than a type | `EXPRESSION_ONLY` |
| `D` | could denote domain, difference, derived object, region or label; no unique referent is recoverable | `UNDERDEFINED` |
| `View` | already registered as source-typed geometry, measurement or composite representation | `EXISTING_TYPE_SUFFICIENT` |

`GPLUS_NOTATION_USEFUL=YES_AS_LOCAL_EXPLICIT_ALIAS_FOR_AUGMENTED_GRAPH`

`GDEGREE_NOTATION_USEFUL=NO`

`G_DEGREE_NOTATION_ADDS_FORMAL_VALUE=NO`

`D_NOTATION_STATUS=UNDERDEFINED`

`VIEW_TYPE_ALREADY_SUFFICIENT=YES`

The phrase “Augmented G+ / G° D / View” supplies no additional formal relation without explicit bindings.
