# RETURN-01 Specification

- Mode: `BOUNDED_FORMAL_RETURN_AND_STATE_HISTORY_CONTROL`
- Date: `2026-08-30`
- Authority: human-authorized bounded Science Lab control
- Question: when does a system return, and exactly which registered component returned?

## Governing distinctions

`RETURN != REPEAT != RESET != RESTART != RECONSTRUCTION`

`STATE RETURN != HISTORY RETURN`

`RETURN IS NOT ERASURE`

The audit uses only standard finite-state, Gregorian-calendar, sequence, phase, validation, representation and graph controls. It compares type structure, not physical mechanisms. All predecessors remain frozen. No external research, code implementation, new operator, new ontology or ORION modification is in scope.

## Method

1. Declare state space, transition, comparison criterion and history representation.
2. Test exact, equivalent, approximate, representational and reconstructed return separately.
3. Keep insertion, perturbation, phase offset and invalid-state requests distinct.
4. Record invalid inputs without repair or hidden interpretation.
5. Assess whether frozen types already express every result.

