# Central Circle Control

The source record does not establish that a seventh, central circle is present. The archived demos contain a central attractor point, not a circle. The known hexagon topology visual contains a central reference node, not a disk definition.

`CENTRAL_CIRCLE_ROLE=UNDERDEFINED`

Possible roles such as fixed reference, boundary constraint, intersection member, redundant region, or visual-only marker cannot be selected without an explicit source definition.

If six outer circles formed one `D6` orbit while a central circle were fixed, the action would be:

- `C_i -> C_(i+1)` under a 60-degree rotation;
- `C_0 -> C_0`.

That would be a six-element orbit plus a fixed element, not sevenfold symmetry.

`SEVEN_CIRCLES_EQUALS_C7_SYMMETRY=NO`

