# Distinct Phase Classes

No phase classes of the requested overlap were computed because `H` is not defined.

Conditional group fact only: if `H` were independently established as a centered `D6`-invariant set, the relative orientation would be identified modulo rotations generated by 72 and 60 degrees, hence modulo 12 degrees. Under that unadopted condition, all sampled values `12 degrees*k` would be rotationally equivalent. Reflections could add the identification `theta ~ -theta`.

This conditional statement does not verify that the current visual defines such a set.

`DISTINCT_RELATIVE_PHASE_CLASS_COUNT=UNDEFINED`
