# nat 0.456 boundary test

The exact label `nat ≈ 0.456` and a producing formula were not located. Earlier sources contain `0.456` as an asserted “Mid-Band,” but provide no distribution, entropy functional, logarithm base, or normalization. The nat interpretation is therefore `UNBOUND_VISUAL_LABEL`.

New arithmetic only:

`0.456=456/1000`, `456+544=1000`, and `544=(-456) mod1000`.

For the CRT register:

`Phi_1428(456)=(1,0,14)`; `S_1428(456)=972`; `Phi_1428(972)=(6,0,3)`. Both reconstruct exactly.

These values are not identified with (-pln p), binary entropy, surprisal (-ln p), or the separately documented (H=3.983066194892531) nats. No distributional meaning is assigned.
