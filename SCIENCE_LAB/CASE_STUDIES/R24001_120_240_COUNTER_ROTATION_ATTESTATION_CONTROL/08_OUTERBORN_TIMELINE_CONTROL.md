# Outerborn Timeline Control

The bounded search did not recover an Outerborn date/axis artifact that explicitly defines the S1 rotation or any other replayable 120°/240° action.

The following boundaries therefore remain controlling:

- `DATE != ANGLE`
- `TIMELINE_POSITION != ROTATION_ANGLE`
- `LABEL_SEQUENCE != GROUP_ACTION`

`OUTERBORN_TIMELINE_TO_ROTATION=NO_MAPPING`.

