# Destruction Controls

| Control | Result | Interpretation |
|---|---|---|
| A — remove 240° | S1 still contains a 120° rotation, but the three-copy completion rule is incomplete | one rotation remains; the tested pair does not |
| B — remove arrows | triangles/layouts do not independently define C3 | `TRIANGLE != C3` |
| C — remove triangle | a formula/angle alone lacks carrier and action unless executable transform syntax remains | formula is not an operator |
| D — reverse arrows | no common historical rule states the resulting semantic change | underdefined outside a typed local action |
| E — relabel TH/Li/Li+ as A/B/C | ordinary mathematical content is unchanged | labels are not required by the formal object |
| F — remove all historical semantics | S1's SVG `rotate` action survives; Outerborn does not | standard local rotation, no special mechanism |

The local S1 result is label-independent. The proposed cross-artifact interpretation is not.

