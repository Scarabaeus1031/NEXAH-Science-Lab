# 3+1+3 Observer Control

No source in the bounded exact-term corpus established that a historical “3+1+3 Observer-Struktur” consists of three equivalent positions acted on by the same 120°/240° operation.

A central node with outer triples is insufficient. In particular:

- `3+1+3 != C7`
- `3+1+3 != C3`
- central placement does not define a rotation center;
- threefold layout does not define an action.

`OBSERVER_313_TO_C3_MAPPING=NO_MAPPING`.

