# Source Image Descriptive Ledger

## Availability finding

The current GARC-01 attachment directory contains only the controlling text file. No image file, checksum, filename, generator record or uniquely identifying path is included with this request.

Earlier conversation visuals are not silently imported because the controlling prompt does not identify which one is the motivating object. Selecting one by resemblance would create provenance rather than verify it.

## Consequently not asserted

- node count;
- edge count;
- whether visible crossings are vertices;
- local valence;
- any measured or intended angle;
- whether markings are labels or geometry;
- whether apparent symmetry is source structure or rendering;
- generator, model, code or data provenance;
- relation to a canonical NEXAH object.

## Classification

`SOURCE_IMAGE_CLASS=CLASS_D_VISUAL_REPRESENTATION_REPORTED_SOURCE_NOT_PHYSICALLY_AVAILABLE`

`SOURCE_IMAGE_GENERATOR_PROVENANCE=UNAVAILABLE`

`SOURCE_IMAGE_RELATION_CLAIM_STATUS=NOT_EVALUATED_IMAGE_UNAVAILABLE`

This absence limits only image-specific claims. It does not block the explicit formal graph counterexample.
