# Historical gate candidate — withdrawn

Gate ID: `ROSC-G10-A`  
Decision: `WITHDRAWN_FROM_ACTIVE_QUEUE / HOMEBASE_PARKED`  
Lab adoption: `NO`

This file preserves the former bounded proposal for provenance. It is not a
current instruction or scheduled operation. The Human Owner subsequently
classified Homebase as private and parked.

## Single question

Does the physical entrance viewpoint attributed to number 35 reproduce the app-displayed horizon targets when observer position, device yaw, pitch, camera height and field of view are declared in one calibrated capture?

## Required inputs

1. outward-facing photograph showing the entrance/gate identity or an official source binding it to 35;
2. coordinate captured at the gate, with device accuracy metadata if available;
3. magnetic and true-north headings, including declination handling;
4. camera height, yaw, pitch and approximate field of view;
5. one panorama export at the same declared orientation;
6. at least three independently identifiable horizon landmarks.

## Metrics

- angular residual per landmark in degrees;
- mean and maximum absolute azimuth residual;
- vertical/elevation residual where available;
- sensitivity to plausible position and heading uncertainty;
- matched control viewpoints displaced along the street.

## Exclusions

Address sums, primes, word images, building-floor semantics and generated visuals do not enter the scoring function.

## Pass condition

Predeclare tolerance only after sensor uncertainty is recorded. A pass establishes repeatable local orientation for the captured viewpoint; it does not establish hidden geometry or causal number encoding.

## Former following gate, also inactive

The neutral five-layer building trace for `KG -> EG -> 1.OG -> 2.OG -> DG` is
also inactive. Neither gate may proceed without a new explicit Human Owner
decision.
