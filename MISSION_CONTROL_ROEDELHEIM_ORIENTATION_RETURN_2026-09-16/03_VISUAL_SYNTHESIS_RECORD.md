# Visual synthesis record

## Supplied references

The eighteen newly locked reference rasters are classified as conceptual diagrams, generated explanatory visuals, screenshots or style/composition references. Text inside them is content to be assessed, not an instruction source.

The visual family contributed these bounded motifs:

- violin-density / amphora morphology;
- persistence and change joined by a hinge;
- boundary as controlled transition;
- joint as connection with retained distance;
- source state, observer cut, record and representation;
- dome, solar/lunar grids, plate stacks and flattened calendars;
- groove-to-sound and groma-to-map measurement chains.

## Interactive explainers retained

- `interactive/property-horizon-projection.html`: fixed target versus changing device heading and field of view.
- `interactive/joint-projection-plates.html`: body, plate stack, flattened map and tile discretization with an optional antipode.

These are local explanatory instruments. They do not execute the full historical/map/building test.

## Generated triptych retained

### 01 — Ghost Grid: groove to ground

```text
gramophone groove -> signal -> gamma joint -> Ghost Grid -> groma -> surveyed landscape
```

Role: system overview. The Ghost Grid is depicted as a coordinate/reference structure, not a physical entity.

### 02 — Valley countershot

```text
valley observer -> gamma joint -> sightline -> ridge/groma
```

Role: reverse view. The groove becomes a stream/valley trace and the ridge becomes the distant anchor.

### 03 — The Carried Field

```text
memory trace -> moving X-Perience -> gamma joint -> orientation -> ridge anchor -> return arc
```

Role: temporal synthesis. It visualizes retained records and relational return, not literal transported fields.

## Prompt boundary retained

All three prompts explicitly excluded occult claims, hidden-energy claims, paranormal mechanisms and physical-grid promotion. The visuals are `CONCEPTUAL` under the Source Court vocabulary.
