# Möbius Sign and Crackline Audit

`1729=7×13×19` is a product of three distinct primes, hence `μ(1729)=(-1)^3=-1`.

`9271=73×127` is a product of two distinct primes, hence `μ(9271)=(-1)^2=+1`.

Therefore, for this specific decimal pair:

`C(1729)=-1`, `C(9271)=+1`, and `μ(reverse_10(1729))=-μ(1729)`.

Classifications: `MOBIUS_SIGN_FLIP_CONFIRMED`, `PAIR_SPECIFIC_RELATION`, `DECIMAL_BASE_DEPENDENT`.

It is not a universal reversal law. The 40 occurrence rows in `03_SEGMENT_NUMBER_THEORY.csv` record source string, partition, position, segment, μ, and class. All three available arithmetic classes occur:

- negative: μ=-1;
- zero: μ=0;
- positive: μ=+1.

The exact class change permits “crackline” only as a new arithmetic class-boundary metaphor. The visual intake contains standard μ notation and drawn/banded divisions, but no visual contains 1729 or 9271 and no generator binds a stripe or plane to these computed classes.

`CRACKLINE_SOURCE_BOUND = PARTIAL`: Möbius classes and boundary vocabulary are source-visible; the 1729→9271 class mapping is `NEW_ADDITIVE_FORMALIZATION`. No geometric, physical, acoustic, or biological separatrix follows.
