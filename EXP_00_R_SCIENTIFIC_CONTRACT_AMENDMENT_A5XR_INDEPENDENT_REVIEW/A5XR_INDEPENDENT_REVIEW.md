# Final Independent Adversarial Review — A5XR

## Decision

**REJECT A5XR — CLASS B DEFECT.** A5XR preserves the accepted science and removes direct authority from producer-supplied P1–P5 Booleans. It does not close the raw-derivation requirement. The canonical fixture contains supplied summary columns and assertions where accepted authority requires reconstructible bootstrap draws, null transformations, N5 transformed rankings, support membership, attribution inputs and provenance.

## Blocking findings

| ID | Class | Finding | Consequence |
|---|---|---|---|
| A5XR-R34 | B | The accepted canonical bundle is not a raw scientific bundle. Nulls are five sets of constant summary columns; bootstrap coefficients and seed directions are constant compressed vectors; N5 has one supplied comparison per transform; mandatory report-only diagnostic results are absent. | Two implementations cannot reconstruct the same null worlds, clustered bootstrap, N5 tau universe or attribution from the supplied artifacts. |
| A5XR-R35 | B | `derive_dominance` raises when exact `2D3>G` or `G<=0`. Accepted authority makes those valid scientific P4 failures; only malformed/fewer-than-three inputs invalidate. | A valid negative P4 result aborts instead of reaching `PARTIALLY REPLICATED` or `NOT REPLICATED`. |
| A5XR-R36 | B | T/F OOS fractions are not derived from support membership; arbitrary unique row prefixes pass; sensitivity `contributing_seeds=30` passes with one `rows_per_seed` key; provenance does not cover observed/bootstrap/attribution/result artifacts. | Support, sensitivity completeness and provenance can be true for mutually inconsistent records. |
| A5XR-R37 | B | A5XR checks only the bytes of `A5X_AUTHORITY_ROOT.json`; it does not execute or reproduce A5X root-member verification. A5XR also reads the live A4 machine for matrices without adding an immutable A4 dependency. | Upstream scientific bytes can change while A5XR's local authority check still appears satisfied. |
| A5XR-R38 | B | Raw failures raise untyped exceptions; the end-to-end runner cannot return `IMPLEMENTATION_FAILURE` or `INVALID EXPERIMENT`. N5-SYNTH and N5-RUN failure are indistinguishable. | The required fail-closed validity state and failure precedence are not mechanically derived. |
| A5XR-R39 | B | The machine contract omits P1–P3, Monte Carlo denominator/tie rule, full bootstrap contract, support thresholds and raw null/N5 schemas. `validate_machine` checks a sealed hash plus four top-level fields, not semantic equivalence. | Coordinated semantic correctness is not independently established; R33 remains open. |

No Class-A ambiguity or unauthorized scientific change was found. This review makes no repair.

## Central falsification

The A5XR runner returns synthetic `REPLICATED` although the bundle has no null replicate worlds, no coefficient-null diagnostics, no bootstrap seed draws, no N5 original/transformed rank pairs, no report-only diagnostic outputs and no canonical provenance ledger. A second implementation following accepted A2–A5 prose must reject that bundle as incomplete. Therefore the two implementations disagree before any scientific result is considered.
