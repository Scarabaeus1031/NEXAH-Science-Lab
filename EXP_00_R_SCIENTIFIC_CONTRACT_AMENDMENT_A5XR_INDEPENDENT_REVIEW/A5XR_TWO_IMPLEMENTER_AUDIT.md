# A5XR Two-Independent-Implementer Audit

## Implementation A — A5XR machine/code

Accepts the canonical bundle, expands constant columns, trusts supplied N5 comparisons and summary metrics, obtains all P propositions true and emits synthetic `REPLICATED`.

## Implementation B — authority/prose reconstruction

Requires raw support membership, 500 seed-cluster draw records, 200 realized null transformations with all mandatory statistics, raw N5 rank comparisons over both representations/query population, per-seed correlation inputs, report-only diagnostic results and a canonical provenance ledger. It rejects the same canonical bundle as incomplete before P1–P5.

Further divergence occurs for a valid exact dominance failure: A aborts; B derives `P4=false` and continues to classification.

The eligible populations, RNG streams and scientific transformations cannot be compared because A's fixture does not contain them. This is a scientifically meaningful output difference while both implementations can cite different controlling layers of the sealed stack.

**TWO-INDEPENDENT-IMPLEMENTERS TEST: FAIL.**
