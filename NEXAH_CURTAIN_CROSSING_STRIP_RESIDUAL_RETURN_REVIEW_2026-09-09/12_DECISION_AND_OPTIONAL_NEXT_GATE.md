# Decision and Optional Next Gate

## Decision

`E_INSUFFICIENT_SOURCE_BINDING_STOP`

Why E:

- Not A: no existing implementation binds state-space gate, crossing, time strip and residual overlay end to end.
- Not B: binding is not merely clerical; primary generators/data/rules are absent.
- Not C: an integration schema is useful, but the mission’s central visual chain cannot be specified as an existing bound relation from current sources.
- Not D: implementation and test are unauthorized and would be premature without source recovery.
- E applies: required physical bindings fail closed.
- Not F: the formal hypotheses are testable; they are unsupported rather than incoherent.

## Exactly one highest-gain integration

Specify, only after a new authorization, one immutable `GateCrossingResidualRecord` with:

`trajectory_hash, generator_hash, config_hash, system_id, coordinates, projection, g_definition, gate_mesh_or_levelset, crossing_rule, event_times, bin_rule, strip_pixel_mapping, residual_type, residual_reference, color_legend, intermediates_hash, result_hash`.

Capability gain: it converts three visually similar layers into one auditable chain or makes their nonconnection machine-explicit. No other proposed integration is advanced.

## Optional next gate

Admit the missing primary source packages without alteration; verify hashes and licenses; bind one system first; perform a no-execution preflight; request separate authority for implementation/test. Failure to supply any required binder returns to E.

