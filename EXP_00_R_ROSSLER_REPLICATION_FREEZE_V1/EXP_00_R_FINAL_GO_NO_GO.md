# EXP-00-R Final GO / NO-GO

## Decision

**NO-GO — DESIGN REVISION REQUIRED**

## Basis

- OD-1 through OD-4 resolved and frozen;
- carrier population selection bias resolved by additive preregistration v1;
- single-pair coherence interpretation corrected and frozen;
- information parity, representation distinctness, and target/actuator audits passed;
- 24 deterministic synthetic/static tests passed;
- registered-seed lock passed;
- immutable configuration and SHA-256 manifest recorded;
- registered Rössler experiment not executed;
- however, the registered runner does not yet orchestrate every frozen null, sensitivity, clustered-bootstrap/per-seed diagnostic, support gate, and final classifier in one sealed end-to-end execution.

## Authorization boundary

No authorization record may be created yet. The current registered/full modes remain locked.

Before another freeze review, create an additive implementation version that:

1. wires the exact action-label-refit, rank-permutation, state-mismatch, support-matched, and coordinate-registration controls at 200 repetitions each;
2. executes every frozen one-factor sensitivity, including both training-seed halves;
3. records the 500-seed-cluster bootstrap interval, per-seed directions, support acceptance gates, and carrier attribution automatically;
4. applies the frozen falsification and cross-system classification rules mechanically;
5. passes a complete non-Rössler synthetic end-to-end runner test;
6. receives new source/config/test hashes and an independent lock review.
