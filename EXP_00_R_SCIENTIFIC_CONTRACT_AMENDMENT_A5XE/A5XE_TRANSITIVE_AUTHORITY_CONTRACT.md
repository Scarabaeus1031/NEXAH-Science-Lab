# A5XE Transitive Authority Contract

A5XE uses a finite authority ledger rooted in the frozen V1 composite and exact controlling A2–A5 scientific artifacts, A5X root/members, A5XR root/members, the A5XR independent-review defect list and all A5XE operative files. Any live upstream file used by reconstruction—including the exact A4 machine containing N5 matrices—is a hashed dependency.

The validator independently recomputes the V1 24-file composite, verifies each explicit upstream byte hash, verifies every A5X and A5XR root member including normalized validators, and verifies the A5XE local root. Merely hashing an upstream root JSON is insufficient.

The external bootstrap anchor is the independently communicated A5XE root SHA-256 embedded in the unchanged validator. A local copied ledger/root update after an upstream mutation cannot change that external expectation. As with A5X, replacing the complete directory and externally trusted validator/root digest is outside self-verification; A5XE claims finite verification, not metaphysical immutability.
