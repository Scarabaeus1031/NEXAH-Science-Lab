# A5XE Raw-Evidence Contract

The controlling flow is raw evidence → reconstruction → statistics → validity → P1–P5 → classification → ceiling. Classification-relevant summaries have no input authority.

The canonical synthetic row table contains exact split/seed/decision row identity, state/covariate inputs, T/F support membership, complete five-action weak ranks, proposals and the shared five-action outcome vector. Canonical identity is the exact Cartesian universe `2 splits × 30 SYNTH seeds × 50 decisions`; foreign identifiers, duplicates, missing rows and noncanonical proposals fail closed.

Support, carrier labels, coherence, top agreement, model inputs, losses, seed directions and dominance are derived from these rows. The fixture's model is the frozen standardized L2-logistic form applied deterministically in canonical order; it is a conformance fixture, not a Rössler implementation.

`summary_only_attack_bundle()` contains plausible observed/null/bootstrap/N5 summaries and positive P/classification assertions but no reconstructible evidence. It must be rejected before classification.
